# CLAUDE.md — SGDP-Service (API .NET 8)

API do SGDP ("API-SUBGD"), sistema em **produção** da SUBGD/GDF. .NET 8 + ASP.NET Core Web API + EF Core 8 (Npgsql/PostgreSQL) + Keycloak. O módulo PGIA (Decreto nº 48.901/2026) é implementado aqui de forma **aditiva e isolada**. Especificações na pasta pai: `analise_integracao_pgia_sgdp.md` (manda em caso de dúvida), `schema_pgia_48901.sql`, `formulario_decreto_48901.md`, `mock_formulario_pgia.html`.

## Regras invioláveis (sistema em produção)

1. **Nunca criar perfis novos nem roles novas no Keycloak.** `GetOrCreateUserAsync` (`app/Repositorio/AuthRepositorio.cs`) sobrescreve `Users.Perfil` com a **primeira** role do token a cada login — role nova no Keycloak pode trocar silenciosamente o perfil de usuários existentes. Papéis do PGIA vivem na coluna nullable `Users.PapelPgia`, administrada por endpoint do próprio sistema. **Não modificar** `GetOrCreateUserAsync`, `app/Auth/Perfis.cs` nem `app/service/PermissionService.cs`.
2. **Nenhuma alteração em tabela existente**, exceto a coluna nullable `PapelPgia` em `Users` (fase 0). Todas as tabelas novas com prefixo `pgia_`. **Nunca editar migrations antigas.** Uma migration nova por fase. Antes de fechar a fase: `dotnet ef migrations script <anterior> <nova>` e conferir que o SQL só cria objetos `pgia_*` (mais o `ALTER TABLE "Users" ADD "PapelPgia"` na fase 0). **O CI (`.github/workflows/apply-migrations.yml`) aplica migrations automaticamente no merge para a main** — já houve migration que quebrou o banco (`app/MIGRATION_INSTRUCTIONS.md`).
3. **Não alterar contrato de endpoint existente.** Exceção permitida: campo `PapelPgia` a mais na resposta do `GET /api/Auth/me` (campo extra não quebra o front).
4. Autorização do PGIA em serviço próprio: `PgiaPermissionService` (papel + ação + recurso + filtro de dados por órgão), espelhando o desenho do `PermissionService` sem tocá-lo. Perfil `admin` do SGDP tem acesso total ao PGIA. **Todo endpoint PGIA valida permissão no backend**; checagem do front é só usabilidade.
5. Endpoints públicos **somente na fase 5**, em controller próprio com `[AllowAnonymous]` explícito, devolvendo apenas os campos do art. 24 (nunca dados pessoais); solicitações do cidadão com validação de entrada e protocolo gerado no servidor.
6. **Não tocar nos testes legados** (`test/` referencia classes que não existem mais — `Projetos`, `CategoriaRepositorio` etc.; o projeto `test/` **não compila**, problema conhecido, fora de escopo). Testes novos do PGIA vivem no projeto separado `test-pgia/` (rodar com `dotnet test test-pgia/test-pgia.csproj`).
7. **Sem dependências novas** salvo estritamente necessário e justificado. QuestPDF já existe para PDFs. **Exceção aprovada (plano da Governança Estratégica, decisão 39): `DocumentFormat.OpenXml` 3.5.1 (licença MIT, o SDK oficial do Open XML mantido pela Microsoft/.NET Foundation), desde a E3, só para gerar o XLSX das planilhas do módulo** (cabeçalho fixo, filtro, números e datas com formato, listas com validação de dados e várias abas, o que um CSV não faz; nada no BCL gera XLSX).

## Isolamento entre módulos e gestão de acessos (rodada de 2026-09-21)

Pedido do usuário: quem entra só pelo login do Keycloak, sem acesso algum, não enxerga nada — nem na tela nem na API — e cada perfil vê apenas os módulos que tem. **Vale para todo controller novo.**

- **Módulos** (`app/Auth/ModulosSgdp.cs`): `demandas` (painel, demandas, entregáveis e os cadastros de apoio Demandante/Esteira/Área executora), `pgia`, `contratacoes` (Supervisão Contínua), `planejamento` (Governança Estratégica, desde 2026-09-24; seção própria no fim deste arquivo), `administracao` (só a role admin).
- **Acesso efetivo = Keycloak + sistema** (decisão do usuário: somar, para ninguém perder acesso no deploy). Regra única: `AcessoModuloService.CalcularModulos`.
  - Keycloak: `admin` abre tudo, `gestor` abre Demandas e a role `pgia` do grupo abre o PGIA como agente do art. 13.
  - Sistema: linhas de `acesso_modulo` com origem `sistema` (Demandas, PGIA e Governança Estratégica) e os papéis de módulo que já existiam. `PapelPgia` válido dá acesso ao PGIA; `PapelContratacoes = ctr_analise` é o acesso à Supervisão Contínua (não há linha de concessão para ele). Na Governança Estratégica o acesso é só a concessão: o papel do módulo (`pe_papel_usuario`) é gravado junto com ela, mas o cálculo do acesso não o lê (regra do deploy).
  - Sem nenhuma fonte, a lista é vazia.
- **Enforcement**: `ModuloAcessoClaimsTransformation` (uma consulta por requisição autenticada; anônima passa direto) põe uma claim `sgdp_modulo` por módulo numa identidade à parte. `ModulosSgdp.AdicionarPoliticas` cria as políticas `modulo:*` (`RequireClaim`).
  - Controllers com a política do módulo: Demanda, Dashboard, Entregáveis, Demandante, Área executora e Esteira → `modulo:demandas`; os 6 controllers PGIA autenticados → `modulo:pgia` (substituiu `Roles = "admin,pgia"`); Processo, Manifestação e Importação de contratações → `modulo:contratacoes`; `PePessoasController`, `PeModeloController`, `PeOrgaosController`, `PeRegistrosController`, `PePeticController`, `PeDeliberacoesController`, `PeArquivosController` e `PePdticController` → `modulo:planejamento`.
  - Continuam `Roles = "admin"`: `PgiaAdminController`, `CtrAdminController` e o novo `AcessoController`.
  - Exceções declaradas: `AuthController` (o `/me` precisa responder a quem não tem acesso), `PedidoAcessoController` (pedir acesso é justamente para quem não tem módulo algum), `AuthLocalController` e `PgiaPublicoController`.
  - **`AcessoModuloTest.TodoControllerExigeOModuloOuOAdmin` falha se aparecer controller sem essa decisão.**
- **Ordem das roles** (`ModulosSgdp.RolesComPerfilPrimeiro`, usado no `OnTokenValidated`): o perfil entra primeiro (admin > gestor > basico, e `basico` sempre existe como fallback), depois as demais roles do token. Motivo: os controllers leem o perfil com `FindFirst(ClaimTypes.Role)`, e um gestor que também tinha a role `pgia` chegava como perfil "pgia" e perdia a edição de demandas. Isso substituiu o "empresta basico só para quem tem pgia".
- **Tabela `acesso_modulo`** (migration **`20260921163934_AcessoModulos`**, script revisado: só `CREATE TABLE acesso_modulo` + PK + 2 CHECKs + FK cascade para `Users` + índice único `(user_id, modulo, origem)`; nenhuma tabela existente alterada). Mapeamento em `Models/Acesso/AcessoModelConfiguration.cs`.
  - Origem `sistema`: concessão da tela (com `concedido_em/por`, é a trilha de auditoria da concessão).
  - Origem `keycloak`: **retrato** das roles no último login, gravado no `/me` (`SincronizarRetratoKeycloakAsync`, só escreve quando muda; corrida de dois logins é absorvida). Serve **só para exibição** — a autorização lê sempre as roles do token da requisição (há teste).
- **`GET /api/Auth/me`** ganhou `Modulos` (lista efetiva; recalculada depois do `GetOrCreateUserAsync`, porque no primeiro login a transformação ainda não acha o usuário). `AcessoPgia` passou a ser o acesso efetivo ao módulo PGIA. `Perfil` sai pela ordem acima.
- **API de gestão** (`AcessoController`, `api/acessos`, admin):
  - `GET usuarios?Filtro=&Modulo=&Page=&PageSize=`: paginada. `Modulo` aceita um módulo ou `nenhum`; valor fora do domínio devolve lista vazia; o filtro usa o retrato para as roles.
  - `GET usuario/{id}`.
  - `PUT usuario/{id}` com `{ Demandas, Pgia, PapelPgia, Contratacoes }`: grava concessões e papéis de uma vez. Tirar o PGIA tira o papel; papel sem o módulo volta 400.
  - Erros saem como **400/404 com `{Code, Message}`** (faixa **9xx**: `AcessoUsuarioNaoEncontrado = 900`, `AcessoInvalido = 901`), não como o 500 herdado.
- **SGDI libera o agente do art. 13**: `PUT api/pgia/governanca/pessoa/{userId}/acesso-modulo` `{ Ativo }` (gate `CanEdit(PessoaVinculo)`). `PgiaPessoaAcesso` ganhou `AcessoModulo` (concessão) e `AcessoModuloKeycloak` (retrato de `pgia` ou de admin), preenchidos em lote pelo controller. O `PgiaGovernancaController` recebe `IAcessoModuloService`.
- **Modo local**: `LoginLocalRequest.ConcederModulos` (`demandas`/`pgia`/`planejamento`) grava concessões de teste (`concedido_por = "modo-local"`); a da Governança Estratégica anda com `PapelPlanejamento` (ver a seção do módulo).
- **Limitação**: uma pessoa com acesso vindo do Keycloak não perde esse acesso pela tela — isso é gerido no Keycloak. A tela mostra a origem.

## Pedidos de acesso (rodada de 2026-09-21, segundo pedido)

Pedido do usuário: o botão "Pedir acesso" do card registra um pedido (módulo + justificativa opcional); a administração aprova, já escolhendo o papel no PGIA e liberando na hora, ou recusa com motivo, que a pessoa lê no card; a SGDI também decide os pedidos do PGIA. Front: seção de mesmo nome no `CLAUDE.md` do SGDP-Angular.

- **Tabela `pedido_acesso`** (migration **`20260921174607_PedidosAcesso`**, script revisado: só `CREATE TABLE pedido_acesso` + PK + 2 CHECKs (módulo em `ModulosSgdp.Liberaveis`, situação em `SituacaoPedidoAcesso.Todas`) + FK cascade para `Users` + `ux_pedido_acesso_pendente` (único `(user_id, modulo) WHERE situacao = 'pendente'`) + `ix_pedido_acesso_situacao (situacao, criado_em)`; nenhuma tabela existente alterada). Colunas: `justificativa` e `motivo_recusa` varchar(500), `criado_em`, `decidido_em`, `decidido_por` (e-mail; **nulo num aprovado = encerrado sozinho**, o acesso chegou por outro caminho), `papel_pgia` (papel dado na aprovação, histórico). Mapeamento em `AcessoModelConfiguration`. **`situacao` é token de concorrência** (`IsConcurrencyToken`).
- **`ModulosSgdp.Liberaveis`** = Demandas, PGIA, Supervisão Contínua e Governança Estratégica (o que o sistema libera; a Administração só vem do Keycloak). `SituacaoPedidoAcesso` (pendente/aprovado/recusado) mora em `ModulosSgdp.cs`, ao lado de `OrigemAcesso`.
- **Quem decide** (`DecisorPedidos`, em `IPedidoAcessoService.cs`): perfil admin decide todos os liberáveis; `PapelPgia = pgia_sgdi` decide o PGIA; `pe_admin` decide a Governança Estratégica (desde 2026-09-24; quem tem os dois papéis decide os dois); o resto não decide nada. O `DecisorAsync` só consulta `pe_papel_usuario` para quem tem a claim do módulo `planejamento` e não é admin.
- **API** (`PedidoAcessoController`, `api/pedidos-acesso`, só `[Authorize]`):
  - `POST` `{ Modulo, Justificativa }`: pede. Módulo que a pessoa já tem, Administração (mensagem explica que vem do Keycloak) ou fora do domínio → 400. Pendente do mesmo módulo → devolve o mesmo (a corrida de dois cliques cai no índice único e também devolve o que ficou).
  - `GET meus`: o último pedido de cada módulo (o card mostra aguardando ou recusado com motivo; `MotivoRecusa` só sai na recusa).
  - `GET ?Situacao=&Modulo=&Filtro=&Page=&PageSize=`: a fila de quem decide (403 para quem não decide). Pendentes primeiro, por ordem de chegada (Id); depois os decididos, do mais novo. Situação ou módulo fora do domínio/escopo → lista vazia. Parâmetro da action chama `consulta` (armadilha do binding).
  - `GET pendentes?modulo=`: `{ Total }` do escopo de quem consulta; **zero para quem não decide** (sem 403, para o contador nunca acionar o interceptor do front).
  - `POST {id}/aprovar` `{ PapelPgia }` e `POST {id}/recusar` `{ Motivo }` (obrigatório, até 500). Erros com `{Code, Message}`: `PedidoAcessoNaoEncontrado = 902` → 404, `PedidoAcessoInvalido = 903` → 400, `PedidoAcessoJaDecidido = 904` → 409, `PedidoAcessoForaDoEscopo = 905` → 403 (SGDI tentando decidir Demandas, por exemplo).
- **Aprovar = a mesma gravação da tela de gestão**: `IAcessoModuloService.PrepararLiberacaoAsync` (novo, **não grava**) deixa pronta a concessão (Demandas/PGIA, com o papel quando vier; papel só vale para o PGIA e aprovar como agente não tira papel existente) ou o `ctr_analise`; o serviço marca o pedido e grava tudo **num `SaveChanges` só**. Duas pessoas decidindo ao mesmo tempo: o token de concorrência recusa a segunda gravação inteira (nem o acesso é liberado) e ela recebe 409.
- **Encerramento automático** (pedido pendente de módulo que a pessoa passou a ter sai da fila como aprovado): `DefinirAcessosAsync` (módulos ligados na tela, com o papel), `DefinirConcessaoAsync` ao ligar ("Liberar como agente" da SGDI), `PUT governanca/pessoa/{id}/vinculo` e `POST governanca/pessoa` quando dão papel PGIA (`EncerrarPedidoPgiaAtendidoAsync` no controller), e o **`/me`** (acesso que chegou pelo Keycloak; `DecididoPor` nulo). Essas gravações toleram o pedido decidido no mesmo instante por outra pessoa (`SalvarAceitandoPedidoJaDecididoAsync`: desanexa o pedido e grava o acesso).
- **Testes**: `test-pgia/PedidoAcessoTest.cs` (35 casos: pedir, idempotência, recusas de domínio, aprovação dos três módulos, papel inválido ou fora do PGIA, conflito, concorrência com dois contextos, escopo da SGDI, quem não decide, ordem e filtros da fila, paginação saneada, os quatro encerramentos automáticos e o controller). O teste de reflexão do isolamento conhece o `PedidoAcessoController` como "autenticado". **Limitação do InMemory**: ele não tem transação, então que "nem o acesso é liberado" na corrida é garantia só do PostgreSQL (o teste confere a recusa e a situação do pedido). 554 testes no `test-pgia`.

## Modelo de papéis PGIA

`Users.PapelPgia` (nullable): `pgia_orgao` (Responsável de IA e equipe do órgão), `pgia_sgdi` (órgão central), `pgia_cgtic` (secretaria do comitê), `pgia_auditoria` (auditora externa) ou `null`. Constantes em `app/Auth/PapeisPgia.cs`. O órgão do usuário resolve-se por `Users.Unidade` → `pgia_orgao.unidade_id`. Escopo: `pgia_orgao` só o próprio órgão; `pgia_sgdi`, `pgia_cgtic` e `admin` veem tudo; `pgia_auditoria` só as auditorias designadas; usuário sem papel só registra uso de IA (art. 13). Atribuição: `PUT api/pgia/admin/papel` com `[Authorize(Roles = "admin")]` (inalterado), `PUT api/pgia/governanca/pessoa/{userId}/vinculo` (papel + unidade, SGDI/admin) ou `POST api/pgia/governanca/pessoa` (pré-cadastro por e-mail, antes do primeiro login).

**Modelo de administração (decisão do dono do produto, a própria SGDI): a SGDI pré-cadastra tudo e entrega o órgão pronto; o papel de órgão só faz as designações.** Vale para os dados do órgão (`orgao_dados`), os dados de agente público (`agente_info`) e o vínculo de acesso das pessoas (`pessoa_vinculo`) — detalhe da matriz no fim deste arquivo.

## Arquitetura e convenções

- Camadas: **Controller fino → Service (interface em `service/Interface`) → Repositório (interface em `Repositorio/Interface`) → `AppDbContext`**. Registrar tudo no `Program.cs` junto dos existentes (`AddScoped`).
- Namespaces reais não seguem as pastas à risca: `Controllers`, `service`, `service.Interface`, `Repositorio`, `Repositorio.Interface`, `Models`, `app.Models` (User/Unidade), `api.<Contexto>`. PGIA usa `Models.Pgia`, `api.Pgia`, `service.Pgia`, `Repositorio.Pgia` (interfaces em `service.Interface` / `Repositorio.Interface`).
- **JSON sai PascalCase como nas classes** (`PropertyNamingPolicy = null` no Program.cs). DTOs PGIA em `app/api/Pgia/` com propriedades PascalCase.
- Erros: `throw new ApiException(ErrorCode.X)`; códigos novos **acrescentados** ao enum `ErrorCode` (`app/api/ErrorCodes.cs`), PGIA usa a faixa 6xx. Não há middleware global de exceção.
- Paginação: `PagedRequest`/`PagedResponse<T>` (`app/api/Common/`).
- Datas puras: `DateOnly`. Timestamps: `DateTime` UTC (`DateTime.UtcNow`); conversor global `BrasiliaDateTimeConverter` cuida do JSON.
- Auditoria nas entidades de negócio: `CriadoEm`/`CriadoPor`/`AlteradoEm`/`AlteradoPor` (e-mail do usuário).
- E-mail do usuário autenticado: `User.FindFirst(ClaimTypes.Email)?.Value` (padrão do `DemandaController` em `Controllers/ProjetoController.cs` — atenção: nomes de arquivo nem sempre batem com a classe).
- Controllers: `[ApiController]`, `[Authorize]` na classe, rota `api/<contexto>` minúscula, comentários `/// <summary>` em PT-BR, permissão checada no início da action (`Unauthorized()` sem e-mail, `Forbid()` sem permissão).

## Entidades PGIA

- Em `app/Models/Pgia/`, com `ToTable("pgia_...")` e colunas snake_case via `HasColumnName` (mapeamento concentrado em `app/Models/Pgia/PgiaModelConfiguration.cs`, chamado pelo `OnModelCreating` do `AppDbContext`).
- Domínios fechados: constantes em `app/Models/Pgia/PgiaDominios.cs` + `HasCheckConstraint` reproduzindo os CHECKs do `schema_pgia_48901.sql`. Nomes explícitos de constraints/índices com prefixo `pgia` (`ck_pgia_*`, `ux_pgia_*`, `ix_pgia_*`, `fk_pgia_*`) para a revisão do script de migration.
- Adaptações do schema validado (seção 4.2 da análise): `agente_publico` **não existe** — usa-se `Users` (Guid) + extensão 1:1 `pgia_agente_info`; `orgao` → `pgia_orgao` com `unidade_id` (FK nullable para `Unidades`); FKs de pessoa são `Guid` → `Users`; **anexos habilitados (rodada de anexos): o binário vai no PRÓPRIO BANCO**, em `pgia_documento_arquivo` (bytea, 1:1 com `pgia_documento`) — a adaptação anterior "upload adiado, só metadados + nº processo SEI" **não vale mais** (o SEI continua sendo o canal oficial de tramitação); views do schema viram queries LINQ nos services. **Divergência registrada:** `pgia_sistema_ia.origem_registro` é `varchar(40)` — o schema declara `varchar(20)`, mas o próprio CHECK dele exige o valor "Instrumento vigente em revisão" (30 caracteres); o `varchar(20)` do schema é bug latente.
- Chaves: `long` identity (`bigint`) nas tabelas `pgia_*`, espelhando o schema.

## Build, testes e migrations

- SDK .NET 8 instalado em escopo de usuário: `$env:USERPROFILE\.dotnet` (adicionar ao PATH da sessão se `dotnet` não for encontrado).
- Build: `dotnet build` em `app/` (baseline: 0 erros, 22 warnings pré-existentes — não introduzir warnings novos).
- **Sem SDK no host?** (é o caso desta máquina desde 2026-09-21: só runtimes) Use a imagem oficial que o `Dockerfile` já usa: `MSYS_NO_PATHCONV=1 docker run --rm -v "<repo>:/src" -v sgdp-nuget:/root/.nuget/packages -w /src mcr.microsoft.com/dotnet/sdk:8.0 dotnet test test-pgia/test-pgia.csproj`. Para `dotnet ef`, instale a ferramenta num volume (`-v sgdp-dotnet-tools:/tools`, `dotnet tool install --tool-path /tools dotnet-ef --version 8.0.13`) e aponte o banco com `-e ConnectionStrings__PostgreSql="Host=host.docker.internal;..."`.
- Testes PGIA: `dotnet test test-pgia/test-pgia.csproj` (xUnit + EF InMemory); módulo Supervisão Contínua das Contratações: `dotnet test test-contratacoes/test-contratacoes.csproj` (mesmo padrão, projeto separado). O projeto legado `test/` não compila — não incluir no build nem tentar consertar.
- Testes do módulo Governança Estratégica: `dotnet test test-planejamento/test-planejamento.csproj` (mesmo padrão, projeto separado, desde 2026-09-24; também no `sonarcloud.yml`).
- Migrations: `dotnet ef migrations add <Nome>` em `app/` (namespace `demanda_service.Migrations`). Última migration pré-PGIA: `20260526120000_ChangeDatesToDateOnly`; última do PGIA: `20260904132953_PgiaAnexosDesignacaoAuditoria`; **última do repositório: `20260924183900_PePdticTrilha`** (Governança Estratégica, E4: cria `pe_pdtic`, `pe_pdtic_passo` e `pe_comentario` e acrescenta a `pe_registro` a coluna nulável `pdtic_id`, o CHECK `ck_pe_registro_dono`, o índice `ux_pe_registro_pdtic_codigo` e a FK para `pe_pdtic`; antes dela, `20260924174627_PeReferenciaisRegistros`, E3, que só cria as 6 tabelas `pe_` dos referenciais e registros; `20260924162542_PeModeloConfiguravel`, E2, que só cria as 13 tabelas `pe_` do modelo configurável, e `20260924151108_PeAcessoPapeis`, que cria `pe_papel_usuario` e `pe_papel_usuario_historico` e recria os CHECKs `ck_acesso_modulo_modulo` e `ck_pedido_acesso_modulo` com `planejamento`, e, `20260923184153_CtrComunicacaoTcdf`, com 4 colunas nullable em `ctr_manifestacao_tcdf` (`processo_comunicacao_tcdf`, `data_recebimento`, `ato_tcdf` e `numero_ato_tcdf`) mais os CHECKs `ck_ctr_manifestacao_ato_tcdf` e `ck_ctr_manifestacao_comunicacao`, `20260922190259_CtrValorHospedagemGdfnet`, com 3 colunas nullable em `ctr_processo`, `valor_estimado`, `hospedagem_cetic` e `usa_gdfnet`, mais o CHECK `ck_ctr_processo_hospedagem_cetic`, `20260922175843_CtrCategoriaServicosTic`, que recria o CHECK `ck_ctr_processo_categoria` com "Serviços de TIC", `20260922140501_CtrCriticidadeCriteriosAssinaturaConclui`, com a coluna jsonb `criticidade_criterios` em `ctr_processo`, `20260921174607_PedidosAcesso`, `20260921163934_AcessoModulos` e `20260915145908_CtrSupervisaoClassificacaoRisco`) (rodada da Supervisão Contínua das Contratações: nome do módulo, Esclarecimentos Adicionais e classificação de riscos; as anteriores do módulo são `20260904153153_CtrAnalisesContratacoes`, `20260911181652_CtrRodadaPlanejamentoTcdf` e `20260911191815_CtrEsclarecimentosPendencias`). Gerar e revisar o script antes de dar a fase por encerrada (`dotnet ef migrations has-pending-model-changes` confere se o modelo e a última migration estão em sincronia).
- O deploy (Dockerfile.prod) compila apenas `app/demanda_service.csproj`; projetos de teste não afetam o CI.

## Estado por fase (atualizar ao encerrar cada fase)

- **Fase 0 (fundação e papéis): implementada, aguardando revisão do usuário.** Coluna `Users.PapelPgia`; `pgia_orgao` (com `ux_pgia_orgao_unidade` único — 1 unidade ↔ 1 órgão), `pgia_agente_info`, `pgia_responsavel_ia`, `pgia_encarregado_dados` (índices únicos parciais `WHERE ativo`), `pgia_prazo_conformidade` (+ seed das 8 obrigações datadas; adesão de órgão instancia as 6 não-SGDI); `PgiaPermissionService` (matriz + escopo por órgão; 47 testes em `test-pgia/`); `PgiaAdminController` e `PgiaOrgaoController`; `PapelPgia` no `/me`. Migration: `20260825185606_PgiaFase0Fundacao`. Detalhes que importam nas próximas fases: índices únicos parciais/de NULL ficam sob o flag `indicesRelacionais` (InMemory não os reproduz); troca de designação usa dois `SaveChanges` (UPDATE do vigente antes do INSERT, por causa do índice parcial); "hoje" para prazos vem de `DateTimeHelper.TodayBrasilia()`; DTOs de escrita levam `[StringLength]` espelhando os varchar; `ApiException` vira 500 sem corpo (sem middleware — comportamento herdado do sistema).
- **Fase 1 (inventário e classificação de risco): implementada, aguardando revisão do usuário.** `pgia_sistema_ia` (sem `contrato_id` — chega na fase 4), `pgia_classificacao_risco` (checklist em jsonb `{"q15":[...]}`; sem `deliberacao_cgtic_id` — chega na fase 2), `pgia_documento` (metadados, sem upload). Risco calculado no servidor (q15→Excessivo, q16→Alto, q17→Moderado, nada→Baixo) com enquadramento derivado do primeiro inciso na ordem do artigo; classificação vigente desnormalizada no sistema; Risco Excessivo bloqueia status em uso na criação/edição — **decisão registrada:** a reclassificação NÃO bloqueia (o inventário registra a realidade; suspensão é remédio das fases 2/3). Condicionais anulam campos de blocos inativos (inclusive supervisão/aviso quando o risco não os exige). `DataClassificacao` default rejeitada; paginação saneada localmente no service (PagedRequest compartilhado intocado). Migration: `20260825194127_PgiaFase1Inventario`. Testes: 93 no `test-pgia/`.
- **Ajustes de desenho da chefia (pós-fase 1, aplicados):** origem do registro ganhou "Outros" + `origem_registro_descricao` (CHECK condicional); questionário sem feedback ao preenchedor; **pesos por quesito** em `PgiaQuesitos.cs` (art. 15=5; art. 16 I/IV/V/VIII/IX=4, II/III/VI/VII=3; art. 17 I/IV=2, II/III=1) somados em `pgia_classificacao_risco.pontuacao_risco`, exposto SÓ a admin/sgdi/cgtic (`incluirPontuacao`); bloqueio Excessivo×em uso **saiu da criação** (anti-manipulação: o registro entra e a SGDI enxerga; bloqueio permanece na transição de status via edição); supervisão/aviso/identificador sempre coletados (sem anulação por risco).
- **Fase 2 (governança central e homologação): implementada.** `pgia_aia`, `pgia_deliberacao_cgtic` (com `documento_id`), `pgia_plataforma_ia_generativa`, `pgia_autorizacao_excepcional` (`autorizada_por varchar(30)` — schema dizia 20, valor não cabia), `pgia_norma_complementar`; homologação em `pgia_sistema_ia` (`situacao_homologacao` + parecer/avaliador/deliberação): rota inicial `SituacaoHomologacao.RotaInicial` (Baixo/Moderado→SGDI, Alto/Excessivo→CGTIC), reclassificação reabre; SGDI avalia via `PUT governanca/sistema/{id}/avaliacao`; deliberação do CGTIC de tipo em `TipoDeliberacao.DecidemHomologacao` sobre sistema delegado aprova/veta (FK circular → dois SaveChanges); **gate de implantação**: transição para em uso exige homologação Aprovado e, para Alto Risco, AIA Concluída+publicada+deliberação Favorável (art. 16, § 1º). `pgia_classificacao_risco.deliberacao_cgtic_id` é coluna preparada (schema, art. 7º III) ainda sem escritor. Migration: `PgiaFase2Governanca` (com **backfill**: sistemas Alto/Excessivo pré-existentes vão à fila do CGTIC via `migrationBuilder.Sql`). Pós-revisão: vínculo de deliberação na AIA e gate exigem `Tipo ∈ DecidemHomologacao` + `SistemaIaId` do próprio sistema; fila de homologação sem N+1 (`ListarClassificacoesVigentesAsync` em lote, ordem por antiguidade); datas default rejeitadas em autorização/norma; autorização revogável (`PgiaAutorizacaoUpdateDTO.Ativo`); `GET governanca/sistemas` (resumo para o CGTIC deliberar sobre sistema já homologado, ex. suspensão do art. 30). 154 testes.
- **Fase 3 (operação contínua): implementada.** `pgia_incidente` (fluxo em duas etapas — **divergência registrada**: `hipotese`/`data_comunicacao_sgdi`/`processo_sei` nullable até a comunicação formal; comunicação pode corrigir as datas aproximadas do aviso), `pgia_nao_conformidade` (origem **forçada por papel**: orgao→SGTIC, sgdi→Supervisão da SGDI; órgão não edita achado da supervisão), `pgia_capacitacao` (único global (agente,trilha) — decisão do schema, vale entre órgãos; `DataConclusao` anulada fora de Concluída), `pgia_registro_uso_ia` (**adaptação**: coluna `orgao_id` persistida; exatamente uma fonte; plataforma por prefixo 'Homologada'; revisão humana obrigatória; listagens paginadas). **Art. 13 sem papel**: `PodeRegistrarUso(ctx)` = órgão resolvido; `GET conformidade/uso/fontes` alimenta os selects sem expor o inventário; capacitação/NC sem paginação por decisão registrada (volume limitado). Migration: `PgiaFase3Operacao`. 198 testes.
- **Fase 4 (contratações, relatórios e auditorias): implementada.** `pgia_contrato_ia` (condicionais do art. 25 pelo risco do sistema vinculado; art. 21 valida órgão/ativo/vigência da autorização), `pgia_instrumento_legado` (art. 37: revisado com IA exige aditivo; `TriadoPor` preservado), `pgia_indicador_desempenho` (**adaptação**: daterange → `periodo_inicio`+`periodo_fim`), `pgia_relatorio_semestral` (**adaptações**: coluna `prazo_envio` persistida = último dia do mês seguinte ao semestre; envio só após o fim do semestre; situação da SGDI com guardas de coerência; conteúdo agregado I–V com janela no dia civil de Brasília; **PDF via QuestPDF** em `PgiaRelatorioPdf` com licença no construtor estático), `pgia_relatorio_anual`, `pgia_auditoria_tecnica` (**adaptação**: `auditor_user_id` → papel `pgia_auditoria` só vê/edita parecer+datas das designadas). `contrato_id` adicionados em deliberação e autorização (schema). Divergência: `sistema_ia.contrato_id` não criada (vínculo vive em `contrato.sistema_ia_id`). Documentos vinculados validam o órgão. Migration: `PgiaFase4Relatorios`. 261 testes. Assimetria estética registrada: GETs de coleção `recurso/orgao/{id}` × POSTs `orgao/{id}/recurso` — não mexer, contrato estabilizado.
- **Fase 5 (transparência pública): implementada — módulo completo.** `pgia_solicitacao_cidadao` (`status varchar(40)` — 3ª correção do bug latente de varchar do schema); `PgiaPublicoController` é o ÚNICO controller sem `[Authorize]`, com `[AllowAnonymous]` explícito por action: Registro Público paginado (só campos do art. 24, sistemas publicados, sem N+1), abertura de solicitação (protocolo `PGIA-{ano}-{6 dígitos}` por `RandomNumberGenerator` com retry de colisão; reclamação de dados nasce encaminhada ao DPO) e consulta por protocolo SEM dados pessoais (teste de minimização por reflexão trava o DTO). Resposta do órgão no `PgiaConformidadeController`. Migration: `PgiaFase5Transparencia`. 280 testes.
- **Revisão da fase 5 (correções aplicadas):** protocolo ganhou sufixo de 4 letras (sem I/O) — `PGIA-{ano}-{6 díg}-{4 letras}`, ~3×10^11/ano, porque ele é a única credencial de acompanhamento e 10^6 era varrível; rate limiter do framework (política `pgia-publico`, 30 req/min por IP via X-Forwarded-For→RemoteIp, 429) SÓ no `PgiaPublicoController` — nenhum endpoint existente passa pelo limitador; `Page` com teto (`int.MaxValue/pageSize` — overflow de `(page-1)*pageSize` devolvia 500); AIA do Registro Público escolhida por `DataPublicacaoPortal` desc (Id só desempata). **Achado pré-existente FORA do módulo: `EsteiraController` sem `[Authorize]` — 4 endpoints `api/Esteira` anônimos em produção. Corrigido a pedido do usuário na branch própria `fix/esteira-authorize` (fora do escopo PGIA; front não sente — o interceptor sempre envia Bearer).**
- **Rodada de usabilidade (correção de contrato do art. 13):** `GET conformidade/uso/fontes` e `GET conformidade/uso/meus` **não têm mais o gate `PodeRegistrarUso`** — unidade sem órgão PGIA recebe 200 com `OrgaoId` nulo e listas vazias (fontes) / lista própria vazia (meus), e a tela mostra o aviso de vínculo. Motivo: o front agora confina todo `basico` a `/pgia/uso`, e o 403 do gate expulsava o agente (interceptor → `/` em branco). O `POST uso` continua com o gate (escrever exige órgão). Confirmado por revisão adversarial + teste vivo.
- **Histórico de decisões do CGTIC (relatório de auditoria, pós-fase 5):** `GET api/pgia/governanca/cgtic/historico` e `.../historico/pdf` no `PgiaGovernancaController` — leitura de escopo central (`CanView(Deliberacao)` + admin/sgdi/cgtic), **sem tabela nova, sem migration**. **CASO** = sistema cuja classificação vigente desnormalizada é Alto Risco ou Risco Excessivo (rota do comitê, `SituacaoHomologacao.RotaInicial`) em situação ∈ {AguardandoCgtic→`Pendente`, Aprovado→`Aprovada`, Vetado→`Negada`}. **`DataEntrada` = `DataClassificacao` da classificação de risco vigente** (maior Id) — é o ato que enquadra o sistema e o delega ao comitê; sem histórico de classificação cai na data de cadastro do sistema no dia civil de Brasília. `Analisadas` conta casos com ≥1 deliberação (inclusive os que seguem pendentes por diligência). `OutrasDeliberacoes` = tudo que o comitê deliberou fora dos casos (sem sistema **ou** sobre sistema fora da rota do CGTIC), `DataDeliberacao` desc — invariante de auditoria: nenhuma deliberação se perde nem se duplica. Ordem dos casos: pendentes primeiro, depois `DataEntrada` desc. Sem N+1: três consultas fixas (casos, datas de classificação em lote, deliberações). PDF em `PgiaCgticHistoricoPdf` (QuestPDF, mesmo padrão do `PgiaRelatorioPdf`; A4 paisagem). **Refatoração interna:** `EhEscopoCentral` virou `public static` em `PgiaGovernancaService` e o helper privado do controller delega a ele — fonte única e testável da regra; call sites das actions vizinhas intactos. 292 testes (`PgiaCgticHistoricoTest`).
- **Revisão do histórico CGTIC (correção aplicada):** a tabela "Demais deliberações" do PDF ganhou a coluna "Sistema (órgão)" — deliberações sobre sistemas fora da rota do comitê (ex.: suspensão de Baixo Risco) caem em `OutrasDeliberacoes` e o objeto precisa aparecer na peça de auditoria, não só no JSON; comentário do DTO alinhado à semântica real.
- **Mudança do modelo de administração (decisão do dono do produto, a SGDI; pós-histórico CGTIC):** *a SGDI pré-cadastra tudo e entrega o órgão pronto; o papel de órgão só faz as designações.* **Matriz** (`PgiaPermissionService`, que é nossa e pode mudar): `orgao_dados` e `agente_info` **saíram** do `CanEdit`/`CanCreate` de `pgia_orgao` (o órgão mantém a **visualização** — `CanView` e `GET orgao/{id}/pessoas` intactos) e passaram a `pgia_sgdi` + `admin`, em **qualquer** órgão (`CanAccessOrgao` já é sempre verdadeiro para o escopo central, então o par de gates das actions não mudou de forma). Recurso **novo** `pessoa_vinculo` (papel PGIA + unidade de um usuário): só `pgia_sgdi`/`admin` no `CanEdit`, **sem visualização própria** — `CanView` é permissivo por papel (órgão e CGTIC dariam `true` em qualquer recurso), então os endpoints de gestão de pessoas usam `CanEdit(PessoaVinculo)` **como gate de leitura também**. `designacao` **não mudou** (órgão no próprio órgão + admin). **Endpoints novos** no `PgiaGovernancaController` (sem tabela nova, sem migration): `GET api/pgia/governanca/pessoas?filtro=&orgaoId=` — todos os usuários do SGDP, **inclusive sem unidade e sem papel** (é assim que a SGDI acha quem acabou de chegar); busca case-insensitive em Nome/E-mail (`ToLower().Contains()`, que vale no Npgsql e no InMemory — `ILike` não valeria), `orgaoId` filtra pela unidade do órgão **ativo**, ordem por Nome e **cap de 200** (revisto para valer sempre — ver revisão adversarial); e `PUT api/pgia/governanca/pessoa/{userId}/vinculo` — corpo `{ PapelPgia, UnidadeId }` com nulos limpando cada campo, papel validado por `PapeisPgia.EhValido`, unidade e usuário conferidos (`PgiaUnidadeNaoEncontrada`/`PgiaUsuarioNaoEncontrado`), **`Perfil` intocado** (mesma regra do `AtribuirPapelAsync`; escrita pelo `PgiaGovernancaRepositorio`, o `AuthRepositorio` não foi tocado). Órgão de cada pessoa resolvido **em lote** (`ListarOrgaosAtivosComUnidadeAsync` + dicionário por unidade), sem N+1. O front do PGIA já esperava exatamente esse contrato (`PgiaPessoaAcesso`/`PgiaPessoaVinculoForm` em `src/api/pgia/interface.ts`). 330 testes (`PgiaGestaoPessoasTest`, mais a matriz atualizada no `PgiaPermissionServiceTest`).
- **Pré-cadastro de pessoa por e-mail (mesmo pedido do dono do produto):** `POST api/pgia/governanca/pessoa`, gate `CanEdit(PessoaVinculo)` (sgdi/admin), corpo `{ Email, Nome, PapelPgia, UnidadeId }` — permite vincular alguém **antes do primeiro login no Keycloak**. E-mail já existente (busca **case-insensitive**, `Email.ToLower()`) **não duplica a pessoa**: aplica papel/unidade no cadastro que já está lá, com a semântica do PUT de vínculo (nulos limpam) e sem reescrever Nome/Perfil. Quando não existe, insere em `Users` com `Perfil = Perfis.Basico` (**placeholder**: o primeiro login sobrescreve com a primeira role do token) e **`KeycloakId` nulo**, para que o **fallback por e-mail do `GetOrCreateUserAsync`** (intocado: busca por `KeycloakId`, depois por `Email`) assuma essa linha e **preserve `Unidade` e `PapelPgia`** — coberto por teste que simula o primeiro login. Validações: e-mail e nome obrigatórios, e-mail com formato mínimo (`MailAddress.TryCreate` + domínio pontuado + sem apelido), papel por `PapeisPgia.EhValido`, unidade existente (`PgiaDominioInvalido`/`PgiaPapelInvalido`/`PgiaUnidadeNaoEncontrada`; nenhum `ErrorCode` novo). Escrita pelo `PgiaGovernancaRepositorio` (`AddUser`), **`AuthRepositorio` intocado**. **Aviso:** o fallback do login compara e-mail **exato**, então pré-cadastro com e-mail de fato divergente do que o Keycloak envia (outro endereço) gera um segundo cadastro no primeiro login — herdado. Caixa diferente **não** é mais problema: a linha nova grava o e-mail em minúsculas (o Keycloak também normaliza), ver revisão adversarial abaixo. 340 testes.
- **Cadastro de órgão pela SGDI (fecha a rodada do modelo de administração):** `POST api/pgia/governanca/orgao` no `PgiaGovernancaController` — **mesmo `PgiaOrgaoCreateDTO` e mesmo `IPgiaAdminService.CriarOrgaoAsync`** do `POST api/pgia/admin/orgao` (validações de sigla/unidade duplicadas e instanciação dos 6 prazos de adesão vêm de graça; `CriadoPor` = e-mail de quem chamou), devolvendo `PgiaOrgaoResponse`. Gate `CanCreate(Orgao)`, que **já era** só sgdi/admin na matriz — nada a ajustar ali. Motivo: o `PgiaAdminController` é `[Authorize(Roles = "admin")]` (intocado, como manda a regra), e sem isso a SGDI dependia do admin do SGDP para abrir órgão. Nenhum contrato existente mudou; o `PgiaGovernancaController` ganhou `IPgiaAdminService` no construtor (já registrado no `Program.cs`). 350 testes.
- **Revisão adversarial da rodada de gestão de pessoas (7 correções aplicadas):**
  - **C1 (destrutivo):** `POST governanca/pessoa` sobre e-mail **já existente** agora aplica só os campos **não nulos** (`AplicarVinculoParcial`): papel/unidade nulos = "não mexer". A semântica "nulo limpa" ficou **só no `PUT vinculo`** (lá é intencional). Sinal de "já existia" via campo **`JaExistia:bool` no próprio `PgiaPessoaAcesso`** (não um wrapper — o front já tipa a resposta do POST como `PgiaPessoaAcesso`; um wrapper quebraria `pgia-governanca.service.ts`); no `GET pessoas`/`PUT vinculo` é sempre false.
  - **C2 (dedup):** a **linha nova** do pré-cadastro grava `Email.ToLowerInvariant()` (o Keycloak normaliza para minúsculas); corrige o dedup do lado do pré-cadastro, sem tocar no `GetOrCreateUserAsync`.
  - **C3 (escopo, falha fechada):** `GetContextAsync` resolve `OrgaoId` só de órgão **`o.Ativo`** — órgão desativado não dá mais acesso ao papel de órgão.
  - **C4 (vazamento/DoS):** o cap de 200 do `GET pessoas` vale **sempre** (renomeado `LimitePessoas`), em qualquer combinação de filtro — `filtro=@` não devolve mais a base inteira.
  - **C5 (beco sem saída):** (a) `POST governanca/orgao` exige `UnidadeId` **não nula** (guard no controller; o admin, intocado, ainda pode criar sem); (b) `PgiaOrgaoDadosDTO` ganhou `UnidadeId` e `AtualizarDadosOrgaoAsync` liga/troca a unidade validando existência + unicidade (1 unidade ↔ 1 órgão) — **nulo mantém a atual** (não desvincula; o front antigo não envia o campo). `PgiaOrgaoService` passou a receber `AppDbContext` (como o `PgiaAdminService`) para validar a unidade; só os 2 sites de teste que o constroem à mão mudaram.
  - **C6 (perda de dados no "Completar dados"):** `PgiaPessoaAcesso` ganhou `Matricula`/`CargoFuncao`/`Vinculo` (de `pgia_agente_info`, em lote no `GET pessoas`; single nos mutations) para o front pré-preencher e não zerar ao salvar.
  - **C7 (integridade art. 10):** `PUT vinculo` e `POST pessoa` recusam (`PgiaDesignacaoVigenteImpedeTroca = 610`, **novo ErrorCode**) mudar a unidade de quem é **Responsável de IA ou Encarregado de Dados vigente** para fora do órgão da designação (deixaria a designação órfã) — mensagem manda encerrar a designação antes. Troca no mesmo órgão (mesma unidade) é liberada. 364 testes.
- **Decisões/limitações registradas nesta revisão (NÃO implementadas):**
  - **`Users.Unidade` é TAMBÉM o filtro de escopo do SGDP legado:** mudar a unidade de um `parceiro`/`basico` muda o que ele enxerga no sistema antigo. Decisão do dono **aceita**; o front avisa na tela de vínculo. A gestão de pessoas do PGIA escreve `Users.Unidade` de propósito (é como o órgão se resolve), então o efeito colateral é inerente ao modelo — não há como isolar sem uma coluna de escopo própria.
  - **Concessão de papel/órgão sem trilha de auditoria:** `Users` não tem `CriadoEm/Por`/`AlteradoEm/Por` e o projeto não injeta `ILogger`; `PUT admin/papel`, `PUT vinculo` e `POST pessoa` **não deixam histórico de quem mudou o quê**. Limitação conhecida; **recomendação futura**: tabela `pgia_acesso_historico` (agente, ação, papel/unidade antes→depois, autor, timestamp) escrita nesses três pontos. Não implementar agora.
  - **Correções de segurança FORA do módulo (a pedido do usuário, nesta mesma branch):** (1) `EsteiraController` ganhou `[Authorize]` na classe (era o único controller anônimo — 4 endpoints alcançáveis sem token); (2) `POST api/Auth/informar-unidade` passou a exigir que o e-mail do corpo seja o do token (`Forbid` caso contrário) — fecha o IDOR em que qualquer autenticado trocava a unidade de terceiros (e, com ela, o escopo de dados). Onboarding da própria unidade segue idêntico (o front sempre manda o próprio e-mail); trocas de unidade alheia continuam só no admin (`PUT modificar-unidade`). Mudanças mínimas e defensivas, sem alterar o formato dos contratos.
- **Questionário agrupado, "Nenhuma das alternativas" e riscos declarados (decisão do dono do produto; reverte em parte o "desagrupado" da rodada da chefia):** o questionário volta a ser **agrupado** por artigo (15/16/17) na tela, cada grupo ganha resposta explícita **"Nenhuma das alternativas acima"**, e entra um grupo **"Outros"** com riscos declarados pelo órgão pela matriz da CGDF. **Sem qualquer texto revelando consequências** (vedação do art. 15, ida ao CGTIC): decisão do dono para não desencorajar resposta honesta — o enforcement continua onde já estava (gate de transição de status, backend). **Entrega 1 — completude:** o jsonb `respostas_checklist` ganhou `q15_nenhuma`/`q16_nenhuma`/`q17_nenhuma` **no mesmo campo** (sem coluna nova); `PgiaChecklistDTO` ganhou os três bools; o service exige, **na criação e na reclassificação**, que cada grupo esteja respondido — **incisos marcados XOR "nenhuma"** (os dois juntos, ou nenhum dos dois, viram `PgiaChecklistInvalido` com mensagem por grupo). O **cálculo não mudou** (q15→Excessivo, q16→Alto, q17→Moderado, nada→Baixo); os "nenhuma" **não pontuam**. Serialização passou de `Dictionary<string,List<string>>` para um `ChecklistPersistido` com `JsonPropertyName` — linhas antigas (sem os bools) desserializam com `false`, compatível para trás (coberto por teste). **Entrega 2 — grupo "Outros":** tabela nova **`pgia_risco_outro`** (FK `classificacao_risco_id` **cascade**, `descricao_risco`/`acao_mitigacao` text obrigatórios, `responsavel_nome`/`responsavel_email` obrigatórios — e-mail validado pela **mesma** regra do pré-cadastro, agora extraída para `PgiaValidacoes.EmailValido`), `probabilidade`/`consequencia` varchar(15) com CHECK. `PgiaDominios.EscalaCgdf` registra a **escala completa da CGDF com pesos 1..5** (Improvável/Raro/Possível/Provável/Quase certo; Desprezível/Menor/Moderada/Maior/Catastrófica) mas o grupo só aceita o **subconjunto** `Permitidos` — probabilidade ∈ {Improvável, Raro, Possível}, consequência ∈ {Desprezível, Menor, Moderada} —, validado **no service (mensagem clara) e no CHECK do banco**. `PgiaClassificacaoCreateDTO.OutrosRiscos` (pode ser vazia) grava os filhos **no mesmo SaveChanges** da classificação (nav `PgiaClassificacaoRisco.OutrosRiscos`); `PgiaClassificacaoResponse.OutrosRiscos` devolve cada um **com Id**, e as três consultas de classificação do repositório ganharam `Include(c => c.OutrosRiscos)` — então a **vigente** (pré-preenchimento da reclassificação e leitura da SGDI) e o histórico expõem os riscos. Os riscos declarados **não entram no cálculo do risco nem na pontuação** (registro complementar do órgão). Migration: **`20260904112137_PgiaQuestionarioRiscosDeclarados`** — script revisado: cria só `pgia_risco_outro` (PK, 2 CHECKs, FK cascade, índice), nenhuma tabela existente tocada. Fixtures antigos precisaram responder os grupos: helper `ChecklistRespondido()` no `PgiaTestBase` marca "nenhuma" nos grupos vazios. 393 testes (`PgiaQuestionarioRiscoTest`).
- **Ajuste do dono do produto — "tudo nenhuma" exige risco declarado:** quando os **três** grupos são respondidos com "Nenhuma das alternativas acima", o grupo "Outros" vira **obrigatório** (ao menos 1 risco) — *sistema sem risco algum não existe*. Regra no `PgiaSistemaService.AvaliarClassificacao`, logo após os `ValidarCompletudeDoGrupo`, valendo em **criar e reclassificar**: `PgiaChecklistInvalido` com "Como nenhuma das situações dos três grupos se aplica, descreva ao menos um risco no grupo \"Outros riscos identificados pelo órgão\"." O checklist com algum inciso marcado continua aceitando `OutrosRiscos` vazio. A regra checa só a **contagem** da lista; o conteúdo de cada risco é validado depois (`MontarOutrosRiscos`), então lista não-vazia com item inválido segue caindo em `PgiaDominioInvalido`. Fixtures da suíte ganharam `RiscoOutroExemplo()`/`OutrosRiscosSeNecessario()` no `PgiaTestBase`, que só acrescentam risco declarado quando os três grupos ficam vazios — preservando o nível de risco que cada teste exercitava. 396 testes.
- **Revisão do questionário (correções + invariantes):** `DescricaoRisco`/`AcaoMitigacao` com `[StringLength(4000)]` e `OutrosRiscos` com `[MaxLength(20)]` — sem isso um único registro inflado degradava a fila de homologação (que carrega os filhos; reproduzido com 2 MB). **Invariante para a futura tela de reclassificação:** classificação anterior ao questionário agrupado desserializa `QxNenhuma=false` com incisos vazios — copy-forward ingênuo é RECUSADO pela completude (comportamento desejado: forçar resposta explícita); a tela deverá tratar linhas antigas (grupos em branco exigindo resposta). **Limitação registrada:** as mensagens de completude/domínio do service saem como 500 sem corpo via HTTP (herança `ApiException` sem middleware) — a UX é coberta pela validação idêntica do front; recomendação futura: exception filter escopado nos controllers `Controllers.Pgia` devolvendo 400 com código+mensagem (não aplicar sem rodada própria — muda o contrato de erro de todos os endpoints PGIA).
- **Anexos de documento (rodada de anexos; substitui a adaptação "upload adiado"):** **decisão do responsável pelo sistema (Márcio/GDF): o binário fica NO BANCO por ora** — a Infra fornecerá um servidor de arquivos depois, e **essa troca será só outra implementação de `IPgiaArquivoStorage`**, sem tocar endpoints, telas nem metadados. *(Durante a rodada chegou-se a desenhar gravação em disco com `Pgia:Armazenamento:Caminho` + volume no Kubernetes; **descartado** — não há chave de configuração de caminho nem pasta `data/`.)* **Modelagem:** o bytea vive em **`pgia_documento_arquivo`** (`documento_id` PK **e** FK CASCADE, `conteudo bytea`), tabela **separada e sem navegação do lado do `PgiaDocumento`** — de propósito: nenhum `Include`/listagem consegue arrastar o blob, e só o download seleciona `conteudo` (teste trava isso por reflexão + inspeção do modelo do EF). Os **metadados** ficam na `pgia_documento`: `content_type varchar(100)` e `tamanho_bytes bigint` novos, e **`orgao_id` virou NULLABLE** — documento de escopo central (ata do CGTIC, relatório anual da SGDI) não tem órgão natural. `TemArquivo` do DTO deriva de `TamanhoBytes != null` (não encosta na tabela do binário). **Endpoints** (`PgiaDocumentoController`, `api/pgia/documento`): `POST` cria o documento em qualquer escopo (`SistemaIaId` herda o órgão do sistema | `OrgaoId` | nenhum = central, só sgdi/cgtic/admin); `POST {id}/arquivo` (multipart, campo `arquivo`, `[RequestSizeLimit]` 25 MB) anexa/**substitui** (upsert no 1:1, binário e metadados no **mesmo SaveChanges**); `GET {id}/arquivo` devolve `FileContentResult` com o ContentType e o nome original no Content-Disposition (404 quando só há metadados). Validações: ≤ 25 MB (conferido no service pelo tamanho real lido), allowlist por extensão minúscula (pdf, doc, docx, odt, xls, xlsx, ods, csv, txt, png, jpg, jpeg), `PgiaArquivoInvalido = 627` (**ErrorCode novo**). **Nome original nunca vira caminho**: é saneado (sem componente de caminho, `..` removido, ≤ 200 chars **preservando a extensão**) e serve só para exibição/Content-Disposition — o endereçamento é pelo id do documento. **Matriz ajustada** (é nossa): `Documento` entrou no `CanCreate` de sgdi/cgtic e no `CanEdit` de orgao/sgdi/cgtic — antes `CanEdit(Documento)` era só admin, o que trancaria o upload para todo mundo; auditoria externa segue fora (nem anexa nem baixa). `POST api/pgia/sistema/{id}/documentos` **não mudou**. Migration única: **`20260904132005_PgiaAnexosDocumento`** — script revisado: `ALTER pgia_documento` (orgao_id nullable + 2 colunas) e `CREATE TABLE pgia_documento_arquivo`, nada fora de `pgia_*`. 427 testes (`PgiaAnexoDocumentoTest`).
- **Adendo dos anexos — FKs de documento que faltavam:** para "habilitar TODOS os campos de anexo" das telas, `pgia_responsavel_ia`, `pgia_encarregado_dados` e `pgia_auditoria_tecnica` ganharam `documento_id` **bigint NULLABLE** com FK **`ON DELETE RESTRICT`** (índice em cada uma). **Restrict, não SetNull nem Cascade:** a cópia do ato (art. 10) e o relatório da auditoria (art. 34) são **prova** — o banco recusa apagar o documento enquanto vinculado, em vez de derrubar a designação (Cascade) ou esvaziar o vínculo em silêncio (SetNull); é também o comportamento de **todas** as outras FKs de documento do módulo (deliberação, AIA, capacitação, autorização). **`pgia_autorizacao_excepcional` NÃO precisou de nada**: já tinha `avaliacao_riscos_doc_id` + `fk_pgia_autorizacao_doc` (Restrict) desde a fase 2, com DTO, validação e response — conferido e coberto por teste. **DTOs aditivos:** `PgiaDesignacaoCreateDTO.DocumentoId` (vale para Responsável e Encarregado) e `PgiaAuditoriaCreateDTO.DocumentoId`, opcionais; os responses devolvem `DocumentoId`. **`DocumentoTemArquivo` foi descartado**: exigiria arrastar o documento em toda listagem — o front resolve pelo `GET api/pgia/documento/{id}/arquivo` (404 = sem anexo) ou pelo `TemArquivo` do próprio documento. **Escopo validado:** designação exige documento **do mesmo órgão** (a cópia do ato é do órgão que designou; documento central não serve); auditoria aceita documento **do órgão do sistema auditado OU central** — quem designa e publica a auditoria é a SGDI, então o relatório pode nascer como peça central dela ou do órgão auditado; o que não vale é documento de um terceiro órgão. Migration: **`20260904132953_PgiaAnexosDesignacaoAuditoria`** — script revisado: só `ALTER TABLE ... ADD documento_id` + índices + FKs RESTRICT nas três tabelas `pgia_*`. 442 testes (`PgiaAnexoVinculoTest`).
- **Revisão adversarial dos anexos (correções aplicadas):**
  - **C1+C2 (crítico):** o **Content-Type do cliente é ignorado por completo** — o MIME sai de um mapa fixo `extensão → MIME` (`PgiaDocumentoService.MimePorExtensao`, as mesmas chaves da allowlist). Antes, o valor cru do cliente quebrava o download **para sempre** (valor sem "/" → `FormatException` no `File()`), estourava o `varchar(100)` com valor longo, e um `text/html` mentiroso era ecoado no download. O `GET {id}/arquivo` passou a mandar **`X-Content-Type-Options: nosniff`**, para o navegador não "corrigir" o tipo por conta própria.
  - **C3 (integridade probatória) — decisão registrada:** a **primeira** anexação segue o gate de escopo do documento; **substituir** arquivo que outra pessoa anexou exige **escopo central (SGDI/CGTIC/admin) ou ser quem enviou o arquivo atual** (`EnviadoPor`). Sem isso, o órgão auditado sobrescrevia o parecer que a SGDI juntou. Regra em `PgiaDocumentoService.PodeSubstituirArquivo` — **fonte única**: o controller a usa para devolver **403** e o service para recusar com mensagem.
  - **C6:** `[RequestSizeLimit]` subiu para **26 MB** (`LimiteRequisicaoBytes`), mantendo o teto de **25 MB no service**. O corpo do multipart tem overhead além do binário, então o limite anterior (igual ao do arquivo) matava 25 MB exatos no model binding com um 400 genérico e tornava as mensagens amigáveis inalcançáveis. **Fronteira:** o teto é **≤ 25 MB**, então 25 MB exatos agora **chegam ao service e são aceitos**; acima disso, recusa do service com a mensagem própria (a revisão pedia recusa em 25 MB exatos — mantido o `≤` do contrato original, com teste dos dois lados da fronteira).
  - **Menores:** `PgiaSistemaService.CriarDocumentoAsync` passou a sanear `NomeArquivo` (mesmo `SanearNomeArquivo`, agora `public static`) e a **ignorar `UrlStorage` vindo do cliente** (é interno do armazenamento); `ValidarCertificadoAsync` (capacitação) ganhou **escopo de órgão**, como os outros quatro validadores de documento; a corrida no **primeiro** anexo (read-then-insert → PK duplicada → 500) agora é capturada (`DbUpdateException`) e **refeita como atualização** via `IPgiaArquivoStorage.ReconciliarConflitoAsync`. *(Nota de teste: o Npgsql envolve a colisão em `DbUpdateException` (23505) — que é o que o service captura; o provedor InMemory levanta `ArgumentException`, então o teste exercita a recuperação, não o tipo.)*
  - **Limitação registrada — memória:** o upload lê o arquivo inteiro para um `byte[]` antes de gravar o bytea, e o EF ainda materializa o valor no comando: **pico ~2× o tamanho do arquivo por requisição concorrente**, com os buffers indo para o LOH (≥ 85 KB). É o limite prático do desenho em banco com teto de 25 MB; some quando o servidor de arquivos da Infra entrar (outra implementação de `IPgiaArquivoStorage`, com stream de ponta a ponta). 461 testes.
- **MODO LOCAL (execução independente sem Keycloak, só testes):** `Auth:ModoLocal=true` → `AuthLocalController` (`api/authlocal/token`, 404 com a flag desligada) emite tokens com o formato dos do Keycloak (claims + `resource_access` — `GetOrCreateUserAsync` e todo o pipeline intactos); `Program.cs` troca só os `TokenValidationParameters` (issuer/chave por processo em `ModoLocalTokens`) e **recusa subir com a flag fora de Development**. **Personas PGIA:** o token aceita `PapelPgia` opcional (null preserva o papel existente — o admin continua mandando) e `VincularAoOrgaoDeTeste`; o controller provisiona unidade + órgão de teste (sigla TESTE, criado pelo `IPgiaAdminService.CriarOrgaoAsync` real, com prazos) antes do `/me` — o `GetOrCreateUserAsync` preserva Unidade/PapelPgia de usuário existente, então nada é desfeito. Usa `AppDbContext` direto (registrado: ferramenta de teste, sem camada própria). Roteiro em `../EXECUTAR-LOCAL.md`.

## Módulo Supervisão Contínua das Contratações (SGDI × TCDF)

Segundo módulo aditivo do repositório, **independente do PGIA** (nada de `pgia_*` é importado por ele). Digitaliza a planilha "Análises de contratações" da SGDI: cada linha é um processo SEI de contratação de TIC que chega para análise e passa por SGDI → SUBGD → UGTIC → Gab SGDI → órgão comunicante, mais as **manifestações da SGDI ao TCDF** no regime de supervisão contínua (Decreto nº 48.899/2026 + IN SGDI nº 1/2026). Contrato de referência: `../desenho_modulo_analises_contratacoes.md` (manda em caso de dúvida); requisitos e o texto integral do despacho: `../prompt_claude_code_analises_contratacoes.md`.

**Regras herdadas que valem igual:** nada no Keycloak; `GetOrCreateUserAsync`, `Perfis.cs` e `PermissionService.cs` intocados; nenhuma alteração em tabela existente além da coluna nullable nova em `Users`; sem dependências novas; `test/` e `test-pgia/` não se tocam.

- **Papel:** coluna nullable `Users.PapelContratacoes` (`varchar(30)`), único valor `ctr_analise` (rótulo "Supervisão contínua das contratações" desde 2026-09-15; antes "Análise de contratações"), constantes em `app/Auth/PapeisContratacoes.cs` (espelho de `PapeisPgia`). Atribuição só pelo admin do SGDP (`PUT api/contratacoes/admin/papel`, `[Authorize(Roles = "admin")]`, escrita direta no `AppDbContext` pelo `CtrAdminService` — o `AuthRepositorio` não é tocado, o `Perfil` nunca muda). `PapelContratacoes` entrou no `GET api/Auth/me` (campo aditivo, como o `PapelPgia`).
- **Tabelas (`app/Models/Contratacoes/`, prefixo `ctr_`, mapeamento em `CtrModelConfiguration`):** `ctr_processo` (número SEI único **entre ativos** — índice parcial `ux_ctr_processo_numero WHERE ativo`, sob o flag `indicesRelacionais` como no PGIA; os 5 checkpoints em `DateOnly?` + `ugtic_nao_se_aplica`; restituição de primeira classe; soft delete + auditoria) e `ctr_manifestacao_tcdf` (N por processo, FK **RESTRICT** — peça de resposta a órgão de controle não cai em cascata). Domínios fechados em `CtrDominios.cs` + CHECKs (categoria, situação no portfólio, criticidade, resultado, desfecho, prazo > 0, e os dois CHECKs de coerência dos incisos I/II e do bloco de riscos).
- **Nada de situação gravada:** `CtrProcessoService.CalcularSituacao` (8 valores desde 2026-09-22: Concluído (contrato assinado) → Restituído → Análise concluída (a devolução ao órgão, que antes se chamava "Concluído") → Retornado ao Gab SGDI → Em análise na UGTIC → na SUBGD → na SGDI → Sem movimentação) e `CtrManifestacaoService.CalcularEstagio` (6 valores) são **fonte única**, `public static`. Os filtros por situação/estágio viram **predicado EF** (`PredicadoSituacao`/`PredicadoEstagio`) — a tabela nunca é materializada para filtrar.
- **Validação única:** `CtrProcessoService.ValidarProcesso(processo, numeroDuplicado)` normaliza (trim, sigla em caixa alta, restituição desmarcada anula data/motivo) e valida formato SEI, unicidade, cronologia dos checkpoints (lacunas permitidas), datas não futuras (`DateTimeHelper.TodayBrasilia()`), UGTIC "não se aplica" × data e restituição. Vale para criar, editar, checkpoint **e importação**. **Disciplina do candidato:** edição, checkpoint e importação validam numa **cópia solta** da entidade e só depois copiam para a rastreada — linha recusada não deixa rastro no contexto (a importação faz um `SaveChanges` só, no fim).
- **Endpoints** (`api/contratacoes`, todos `[Authorize]`, cada action resolvendo `CtrPermissionService.GetContextAsync` + `CanView`/`CanEdit`): `processo` (lista paginada com busca e filtros, `siglas`, `exportar`, CRUD com soft delete, `PUT {id}/checkpoint`, `GET {id}/manifestacoes`, `POST {id}/manifestacao`), `painel` (rota própria declarada no `CtrProcessoController` com `~/api/contratacoes/painel`), `manifestacao` (lista com filtro por estágio, `GET`/`PUT {id}`, `GET {id}/despacho/pdf?local=&nome=&cargo=`) e `importacao` (`POST previa` e `POST`, multipart campo `arquivo`, 5 MB, só `.csv`). Paginação **saneada no service** (piso/teto, `Page` com teto `int.MaxValue / pageSize`); `PagedRequest` compartilhado intocado. `TotalManifestacoes`/`UltimoEstagioTcdf` resolvidos em lote (sem N+1).
- **ARMADILHA de model binding (defeito confirmado ao vivo e corrigido):** nas actions que recebem um modelo de query (`CtrProcessoController.Listar`/`Exportar` e `CtrManifestacaoController.Listar`) o parâmetro **não pode se chamar `filtro`**. O binder de tipo complexo procura primeiro o prefixo com o nome do parâmetro; encontrando a chave `Filtro` na query — que é uma **propriedade** do modelo — ele passa a exigir `filtro.Filtro`, `filtro.Page`, `filtro.Sigla`... e, como ninguém manda assim, **todas** as propriedades ficam no default: a listagem devolvia a base inteira ignorando busca, filtros e paginação, sem erro nenhum. Os três parâmetros passaram a se chamar **`consulta`**. Vale para qualquer modelo de query novo: o nome do parâmetro nunca pode coincidir (case-insensitive) com uma propriedade dele. Travado por reflexão em `CtrBindingQueryTest`. Parâmetros de tipo simples (ex.: `Painel([FromQuery] int diasSemMovimento)`) não sofrem disso.
- **Matriz:** `CtrPermissionService` nasce papel × ação × recurso (`processo`, `manifestacao`, `painel`, `importacao`, `papel`), mas **sem recorte por órgão** — quem entra (admin ou `ctr_analise`) vê e gere tudo: é ferramenta central da SGDI. `CanCreate` não existe de propósito: criar e editar são a mesma permissão (`CanEdit`).
- **CSV (`CtrCsv`, puro e testável):** leitura com BOM UTF-8 como atalho, depois **UTF-8 estrito** (`new UTF8Encoding(false, true)`, que lança em byte inválido) e só então **Windows-1252** — assim o CSV do Google Sheets/LibreOffice (UTF-8 sem BOM) entra sem mojibake e a planilha real em 1252 continua correta (`CodePagesEncodingProvider` registrado no construtor estático — vem do runtime, sem pacote novo); `;`, aspas com `""` que **só abrem campo vazio** (RFC 4180 — aspas no meio do texto, como a polegada de um rack 24", são literais), título antes do cabeçalho, colunas **por posição**, `-` na UGTIC = "não se aplica", categoria vazia = `Sem objeto / Indefinido`, categoria comparada por normalização (acentos/caixa/espaços). Escrita em **UTF-8 com BOM**, `;`, CRLF, as 12 colunas originais + `Restituído`/`Data da restituição`/`Motivo da restituição` — o export **reimporta sem perda** (round-trip coberto por teste).
- **Restituição inferida da observação** (planilha legada): observação que menciona "restituído" vira `Restituido` + data + motivo = a observação inteira, com a `Observacao` preservada íntegra. A menção **negada** ("não restituído", "não foi restituída") é descartada; a data preferida é a da **mesma frase** da menção (até o próximo `.`/`;`) e não a primeira do texto — "Recebido em 01/02. Restituído em 05/05" grava 05/05; sem data na frase, cai para a chegada à SGDI ou a última data preenchida; `dd/MM` completa o ano com o da chegada à SGDI e, se isso cair no futuro, usa o **ano anterior**. **Divergência registrada:** o desenho fixa o regex `restitu[ií]d[oa]`, mas a planilha real escreve "resti**u**ído" (sem o segundo "t") em uma das duas restituições — o "t" virou opcional (`restit?u[ií]d[oa]`, superconjunto do contratado), senão aquela linha entraria sem restituição e o aceite das "duas restituições" falharia.
- **Despacho ao TCDF em PDF:** `CtrDespachoTextos` guarda os parágrafos **na literalidade** do documento oficial (teste confere palavra por palavra) e `CtrDespachoPdf` (QuestPDF, licença no construtor estático como o `PgiaRelatorioPdf`) monta o A4 com **todas** as alternativas do formulário, `( X )` nas marcadas e `(   )` nas demais, colchetes preenchidos. Local/nome/cargo vêm na query e **não são persistidos**. **Revisão sobre PDF gerado ao vivo (aplicada):** a `Observacao` da manifestação **não é impressa** — é anotação interna da equipe e o template do TCDF não tem esse campo (teste prova pelo tamanho idêntico do PDF com e sem observação); o **fecho** ("À consideração superior." + local + data + nome — cargo) é um bloco único com **`.ShowEntire()`**, colado ao último parágrafo, e o ritmo vertical foi apertado (`Spacing(4)`, `PaddingVertical(8)`) porque com 8pt o despacho estourava para uma segunda página e o fecho ficava **órfão** lá — teste trava em **1 página** no pior caso (inciso I com riscos e o objeto mais longo da planilha real). O cabeçalho é `page.Header()`, então se repetiria em eventual página 2 (aceito).
- **Revisão adversarial do backend (correções aplicadas, uma por teste):** **A1** aspas só abrem campo vazio (antes uma polegada no meio do objeto engolia todas as linhas seguintes num campo só, em silêncio). **M1** `CtrManifestacaoService.AtualizarAsync` valida num **clone** e só depois copia — PUT recusado não deixa mais a entidade rastreada com o estado inválido (a mesma disciplina que o processo já tinha). **M2** restituição negada não vira restituição e a data sai da frase da menção (acima). **M3** o número do processo só é "ocupado" no dicionário do parser quando a linha sai **íntegra**: primeira ocorrência rejeitada (ex.: categoria desconhecida) não faz a segunda, válida, ser descartada como duplicada. **B1 (CSV injection)** célula de **texto livre** (órgão, sigla, complemento, objeto, observação, motivo) que começa com `=`, `+`, `-`, `@`, TAB ou CR sai do export com apóstrofo à frente, e a importação remove **um** apóstrofo apenas quando ele antecede justamente esses caracteres (round-trip fiel, texto que começa com apóstrofo intacto); datas, o `-` da UGTIC e o Sim/Não **não** são neutralizados. **B2** `dd/MM` que cairia no futuro usa o ano anterior (antes a linha era rejeitada por data futura). **B3** `ComunicadaDesde` tem de ser ≤ hoje **e** ≤ `DataOficio` (o inciso I afirma que a contratação já estava no monitoramento quando o TCDF comunicou). **B4** encoding acima. **B5** `Situacao`/`Estagio` fora do domínio devolvem **lista vazia** — filtro que o servidor não entende não pode virar "todos" em silêncio (só valor ausente/em branco é "sem filtro"). **B6** `ListarManifestacoesDoProcessoAsync` filtra processo **ativo**, como as demais leituras. **B7** informar a data da UGTIC **desmarca** o "não se aplica" (informar a data é o gesto de reativar a etapa) em vez de estourar validação. **B8** `GetAtivoPorNumeroAsync` (código morto) removido do repositório e da interface; `PodeAcessar` fica (é contrato do front). **B9** o filtro `Restituidos` compara `bool` com `bool` (`.Value` dentro do `if`), sem depender da semântica de nulo do provider. **B10** `PorSituacao` desempata pela ordem do domínio (do trâmite) e `PorCategoria`/`PorOrgao` por `Chave`.
- **Erros:** faixa **8xx** do `ErrorCode` (800 a 809). Herança conhecida: `ApiException` vira **500 sem corpo** via HTTP (sem middleware) — o front replica as validações para a UX.
- **Modo local:** `AuthLocalController.LoginLocalRequest` aceita `PapelContratacoes` (validado por `PapeisContratacoes.EhValido`), provisiona junto a unidade central de teste (o `userConfiguredGuard` do front exige unidade) e preserva o papel quando o campo vem nulo. Persona `contratacoes@local.teste` no roteiro do `../EXECUTAR-LOCAL.md`.
- **Migration:** `20260904153153_CtrAnalisesContratacoes` — script revisado: só `ALTER TABLE "Users" ADD "PapelContratacoes"` + `CREATE TABLE ctr_processo` + `CREATE TABLE ctr_manifestacao_tcdf` + os índices `ctr_*`. Nenhuma tabela existente tocada.
- **Testes:** `test-contratacoes/` (xUnit + EF InMemory; `CtrTestBase` com admin / analista `basico`+`ctr_analise` / `basico` sem papel / `gestor` sem papel): **319 testes** (147 da entrega original + 51 da rodada da chefia + 42 da rodada de esclarecimentos/pendências + 94 da rodada da Supervisão Contínua, com saldo de -15 na rodada dos riscos da contratação, que tirou os testes do questionário do PGIA): matriz de permissão, processo (formato, duplicidade entre ativos e reaproveitamento do número após soft delete, cronologia, data futura, UGTIC n/a × data, restituição, as 7 situações, dias sem movimento), listagem (filtros, busca, paginação saneada, ordenação, siglas), checkpoint, painel (contagens, médias com amostra conhecida, gargalos), parser (fixtures em **bytes Windows-1252** e em UTF-8 com BOM de 15 colunas), round-trip export→prévia, importação, manifestação (incisos, riscos, blocos inativos recusados, estágio, processo excluído → some das listas e 404), despacho (literalidade + PDF nos dois incisos e em cada desfecho), a trava de binding de query, as duas regressões do PDF (observação fora, fecho inteiro em uma página) e as correções da revisão adversarial (`CtrRevisaoAdversarialTest`). **A planilha real da equipe está versionada em `test-contratacoes/Fixtures/planilha-analises-contratacoes.csv`** (bytes originais): é o teste de aceite: 37 processos criados, zero rejeição, as duas restituições reconhecidas e nenhuma duplicação na segunda importação.
- **Rodada da chefia (2026-09-11) — devolução dispensada, fase, criticidade no processo, status no TCDF e origem:** contrato em `../desenho_rodada_chefia_contratacoes.md`. (1) **`retorno_orgao_nao_se_aplica`** (CHECK `ck_ctr_processo_retorno_orgao`, espelhado no service): `CalcularSituacao` conclui com `RetornoOrgao != null` **OU** (`RetornoOrgaoNaoSeAplica && RetornoGabSgdi != null`) — marcar "não se aplica" **sozinho não conclui nada** (sem o retorno ao Gab o processo segue onde está), e por isso o `PredicadoSituacao` de `RetornadoGabSgdi` ganhou `&& !p.RetornoOrgaoNaoSeAplica`; `CtrCheckpointDTO.NaoSeAplica` passou a valer para `ChegadaUgtic` **e** `RetornoOrgao`, com a mesma simetria da B7 (informar a data desmarca o flag); desmarcar **não** apaga a data. (2.1) **`etapa_planejamento`** (DFD/ETP/TR) + **`data_assinatura_contrato`** → **`Fase` DERIVADA** (`CtrProcessoService.CalcularFase`, `public static`: assinado = Execução), filtros `Fase?`/`EtapaPlanejamento?` (fora do domínio = lista vazia, regra B5) e bloco **`PorFase`** no painel (as 2 fases sempre, quantidade desc + ordem do domínio). **A assinatura NÃO entra na cronologia do trâmite** — o TCDF analisa contrato já assinado, então assinatura anterior à chegada na SGDI é legítima; só não pode ser futura. A etapa **não** é anulada pela assinatura. (2.2) **Criticidade saiu da manifestação e virou do processo** (`ctr_processo.criticidade` + `ck_ctr_processo_criticidade`; a coluna da manifestação foi **removida**): `ValidarManifestacao` passou a receber `criticidadeProcesso` e o inciso I exige que **o processo** a tenha ("Defina a criticidade no cadastro do processo antes de registrar a manifestação do inciso I."); `CtrManifestacaoCreateDTO/UpdateDTO` **perderam** `Criticidade` e o `Response` a mantém como **espelho somente leitura** de `m.Processo?.Criticidade` (vale nos dois incisos — o PDF é que condiciona ao inciso I); `CtrDespachoPdf` lê de `processo.Criticidade`; `ck_ctr_manifestacao_inciso` recriado sem `criticidade IS NOT NULL` e `ck_ctr_manifestacao_criticidade` removido. (3) **`status_tcdf`** (`Suspenso por irregularidades` | `Edital revogado`, CHECK `ck_ctr_manifestacao_status`, **fora** dos CHECKs condicionais dos incisos — é fato do Tribunal): **precedência** no `CalcularEstagio`, `Estagio.Todos` com **8** valores (os dois novos no fim) e `PredicadoEstagio` dos seis estágios de análise agora exige `m.StatusTcdf == null` (senão a precedência valeria só no cálculo e não no filtro); **não entra no PDF**, como a Observação. (4) **`origem`** (`Órgão comunicante` default | `TCDF`, CHECK `ck_ctr_processo_origem`), opcional nos DTOs (vazia = órgão comunicante), filtro próprio, **sem endpoint novo** — processo de origem TCDF nasce sem data nenhuma e já assinado, e nenhuma validação impede isso. **CSV:** cabeçalho com 19 colunas, as 4 novas **ao final** (`Etapa do planejamento;Assinatura do contrato;Criticidade;Origem`), opcionais na importação (ausentes/vazias = comportamento antigo) e rejeitando a linha com o nome da coluna quando fora do domínio; o `-` passou a valer também na coluna de retorno ao órgão (leitura e escrita). Migration única **`20260911181652_CtrRodadaPlanejamentoTcdf`** — **ordem escrita à mão** (o scaffold punha o DROP antes das colunas novas, o que mataria o backfill): colunas do processo → `status_tcdf` → **backfill** (`migrationBuilder.Sql`, `UPDATE ... FROM (SELECT DISTINCT ON (m.processo_id) ... ORDER BY m.processo_id, m.data_oficio DESC, m.id DESC)`) → CHECKs → `DROP COLUMN criticidade`; o `Down` ganhou o **backfill reverso** antes de `ctr_processo.criticidade` sumir, senão o CHECK antigo não poderia ser recriado. Script revisado: só objetos `ctr_*`. **Limitação registrada:** o backfill é SQL do PostgreSQL (`DISTINCT ON`) e o InMemory não roda migration — a cobertura é a inspeção das operações em `CtrMigrationRodadaChefiaTest` (ordem + conteúdo do SQL + só tabelas `ctr_*`), feita **por reflexão** porque o projeto de teste não referencia `EntityFrameworkCore.Relational` (versão diferente da resolvida pelo app; acrescentar o pacote seria dependência nova). **192 testes** (`CtrRodadaChefiaTest` + `CtrMigrationRodadaChefiaTest`); `SemearProcesso` passou a nascer com criticidade Alta e `NovaManifestacaoIncisoI` perdeu o campo. **ARMADILHA nova (flakiness real, capturada em 1 de 3 execuções):** os testes que provam que um campo NÃO é impresso comparando o **tamanho** de dois PDFs (a Observação, e agora o Status no TCDF) falham de forma intermitente quando **duas classes geram PDF em paralelo** — o QuestPDF/SkiaSharp tem estado de processo (cache de fontes, subconjunto de glifos embutido), e o mesmo conteúdo sai com alguns bytes de diferença. `CtrDespachoPdfTest` e `CtrRodadaChefiaTest` agora vivem na mesma coleção (`CtrDespachoPdfCollection`, `DisableParallelization`), o que serializa as duas e devolve o determinismo (6 execuções seguidas verdes). **Qualquer classe nova que gere despacho em PDF tem de entrar nessa coleção.**
- **Correção de PERDA DE DADOS na importação (confirmada ao vivo na mesma rodada) — coluna AUSENTE ≠ coluna VAZIA:** reimportar a planilha real da equipe (**12 colunas**, que não sabem nada da rodada da chefia) **zerava** `criticidade`, `etapa_planejamento`, `data_assinatura_contrato` e `origem` digitados na tela — **inclusive a criticidade que o backfill da migration gravou** —, e como a criticidade passou a ser exigida no inciso I, o fluxo do TCDF quebrava logo em seguida. A semântica das **4 colunas opcionais** passou a distinguir três casos: **coluna ausente do cabeçalho** → no upsert de processo **existente** o campo **não é tocado** (preserva o banco), e em processo **novo** vale o default de sempre (nulo / `Órgão comunicante`); **coluna presente e célula vazia** → **limpa** o campo (é escolha explícita de quem exportou, e é o que mantém o round-trip export→importação fiel); **coluna presente com valor fora do domínio** → linha rejeitada nomeando a coluna, como já era. As 12 colunas originais (datas, observação) e as 3 da restituição **continuam sempre aplicadas** — nelas a planilha é a fonte. **Desenho:** `CtrCsv.Ler` deriva do **CABEÇALHO** (não das linhas de dados: célula a mais numa linha não inventa coluna) um `CtrCsvColunasOpcionais` — 4 bools, default `Nenhuma` — que viaja em cada `CtrCsvLinha.ColunasOpcionais`; o parser **nem lê** a posição de uma coluna ausente; e `CtrImportacaoService.PreservarColunasAusentes` herda do processo gravado, **antes** de validar, os campos cujas colunas o arquivo não traz (assim a prévia mostra o resultado real e a validação enxerga o estado final). Os DTOs de resposta da prévia **não mudaram**. 198 testes — 6 novos, incluindo a repro com a planilha real; os dois principais foram conferidos falhando com a correção desativada.
- **Rodada de esclarecimentos, pendências do TCDF e correções da revisão (2026-09-11):** contrato em `../desenho_esclarecimentos_pendencias.md`. **A1 — pedido de esclarecimentos ao órgão:** `ctr_processo` ganhou `esclarecimento_solicitado_em`/`esclarecimento_descricao`/`esclarecimento_respondido_em` + CHECK `ck_ctr_processo_esclarecimento` (resposta sem pedido não existe), espelhado no service; as datas não podem ser futuras e a resposta não pode anteceder o pedido. **Normalização × validação (ajuste confirmado ao vivo):** sem a data do pedido, a **descrição** é anulada (é gesto explícito do usuário limpando o bloco, não erro), mas a **resposta** preenchida é **RECUSADA** — "Informe a data do pedido de esclarecimento antes de registrar a resposta." Anular a resposta em silêncio, como era, devolvia HTTP 200 escondendo perda de dado: na importação, uma planilha com a coluna *Esclarecimento respondido em* preenchida e a de pedido vazia entrava como "Atualizar" e a data sumia sem nada aparecer na prévia. A validação é **fonte única** (`ValidarProcesso`): a importação só propaga `ex.Error.Message`, então a prévia mostra a linha como "Rejeitar" com a mesma frase. **É sinal PARALELO: a situação do trâmite NÃO muda** — mas `UltimaMovimentacao` passou a considerar as duas datas (pedir e responder É movimentação, e portanto zera os dias parados). Derivados `public static` no `CtrProcessoService`: `CalcularEsclarecimentoPendente` e `CalcularDiasEsclarecimentoPendente`, expostos como `EsclarecimentoPendente`/`DiasEsclarecimentoPendente`; filtro `EsclarecimentoPendente?` via `PredicadoEsclarecimentoPendente` (predicado EF, sem materializar) e `TotalEsclarecimentoPendente` no painel. **A2 — pendências do TCDF:** `ctr_manifestacao_tcdf.pendencias_tcdf` text, **sem CHECK condicional** (vale em qualquer inciso), `[StringLength(4000)]`, vazio normalizado para nulo, **fora do PDF** (terceira ocorrência da mesma decisão: Observação, StatusTcdf e agora PendenciasTcdf). **CSV:** cabeçalho com **22** colunas — as 3 do esclarecimento ao final —, tratadas como **BLOCO** em `CtrCsvColunasOpcionais.Esclarecimento` (presente só quando as três estão no cabeçalho): herdar parte do bloco produziria estado que o CHECK recusa. A descrição é coluna de texto livre, logo entra na proteção contra CSV injection.
  **Correções da revisão adversarial (parte B, backend — cada uma conferida falhando com a correção desativada):** **A1** a coluna *Data de Retorno ao Órgão Comunicante* **existe** na planilha legada, então a proteção por coluna ausente não a cobria e reimportar zerava `retorno_orgao_nao_se_aplica` (o processo saía de Concluído). A célula passou a ter **três** estados: `-` marca, data explícita desmarca, **vazia preserva** o flag gravado — sinalizado por `CtrCsvLinha.RetornoOrgaoNaoInformado` e aplicado no upsert. Teste usa a planilha real (processo `00220-00008043/2026-13`, que tem retorno ao Gab e a coluna do órgão vazia). **A2** remover a criticidade de processo que já tem manifestação do **inciso I** é recusado (`CtrProcessoService.ValidarCriticidadeNaoRemovida`, fonte única usada pelo `PUT` **e** pela importação, que rejeita a linha com a mesma mensagem) — sem isso o despacho saía com o `[Alta/Média/Baixa]` do papel; como cinto de segurança, `GerarDespachoPdfAsync` recusa o inciso I sem criticidade. Protege também o `Down` da migration anterior, que recria o CHECK antigo exigindo criticidade. Duas consultas novas no repositório (`TemManifestacaoIncisoIAsync` para o PUT, `ListarProcessosComIncisoIAsync` em lote para a importação — sem N+1). **M1** `AtualizarAsync` com `Origem` nula/vazia **preserva** a gravada; a regra "vazio = Órgão comunicante" ficou **só na criação** (o front antigo não manda o campo, e resetar a origem de um processo do TCDF numa edição qualquer é perda silenciosa). **B3** `-` na coluna *Assinatura do contrato* **rejeita a linha** nomeando a coluna (não existe "não se aplica" para assinatura). **Assimetria corrigida:** `CalcularEstagio` passou a testar `StatusTcdf != null`, igual ao `PredicadoEstagio` (o valor já é normalizado no `AplicarDto`; cálculo e filtro não podem divergir). **Contrato do checkpoint travado dos dois lados (C1/B4):** `NaoSeAplica` só é aceito em `ChegadaUgtic` e `RetornoOrgao`; nas outras três etapas **qualquer valor não nulo é recusado** — mandar `false` devolvia 500 no primeiro uso do modal, e o front passou a **omitir a chave**. Três teorias cobrem as cinco etapas (recusa do `false` onde não vale, aceite onde vale, e a chave omitida funcionando em todas), mais o caso do B4: limpar a data do retorno com a chave omitida **não** desmarca o "não se aplica". Migration única **`20260911191815_CtrEsclarecimentosPendencias`** (scaffold limpo, sem reordenação manual): 3 colunas em `ctr_processo`, `pendencias_tcdf` em `ctr_manifestacao_tcdf` e o CHECK — script revisado, só objetos `ctr_*`. **240 testes** (`CtrEsclarecimentosPendenciasTest`, que entrou na coleção `CtrDespachoPdfCollection` por gerar PDF).
- **Rodada da Supervisão Contínua das Contratações (2026-09-15) — nome do módulo, Esclarecimentos Adicionais e classificação de riscos:** contrato em `../desenho_supervisao_riscos.md`. (1) **Nome exibido** passou a "Supervisão Contínua das Contratações" (comentários, mensagens, docs e o arquivo do export, `CtrProcessoController.NomeArquivoExportacao` = `supervisao-continua-contratacoes.csv`); **identificadores técnicos não mudaram** (rota `/analises`, `api/contratacoes`, prefixo `ctr_`, papel `ctr_analise`, `Users.PapelContratacoes`, classes `Ctr*`, migrations antigas); a planilha legada continua se chamando "Análises de contratações" (parser intocado). (2) **`PendenciasTcdf` → `EsclarecimentosAdicionais`** de ponta a ponta (entidade, DTOs, service, testes) e coluna `pendencias_tcdf` → `esclarecimentos_adicionais` por **`RenameColumn`** (dados preservados; semântica intacta: ≤ 4000, qualquer inciso, vazio = nulo, fora do PDF). (3) **Classificação de riscos no processo:** colunas `checklist_risco` (jsonb no formato de `respostas_checklist`), `risco_classificado` (CHECK nos 4 resultados do PGIA), `enquadramento_risco`, `pontuacao_risco`, `risco_classificado_em/por` + CHECK tudo-ou-nada `ck_ctr_processo_classificacao`; tabela **`ctr_risco_declarado`** (FK **CASCADE**, CHECKs na **escala COMPLETA** da CGDF — o grupo "Outros" do PGIA aceita só o quadrante baixo). **Reuso só de fontes estáticas do PGIA** (`PgiaQuesitos`, `ChecklistIncisos`, `ResultadoRisco`, `EscalaCgdf`, `PgiaValidacoes.EmailValido`): a regra privada `PgiaSistemaService.AvaliarClassificacao` foi **replicada** em `CtrClassificacaoRisco` (fonte única do módulo: `Avaliar` pura, `CalcularResultado`, `CalcularNivel` com a matriz 5×5 na disposição de `MATRIZ_RISCO_CGDF`, `CalcularNivelMaximo`, `MesmaClassificacao` e os predicados EF) e a **equivalência é provada por teste que invoca a regra do PGIA por reflexão** (resultado, enquadramento, pontuação e o jsonb byte a byte, mais as mesmas recusas) — nenhum arquivo `Pgia` alterado (conferido por data de modificação). `CtrDominios.NivelRisco` (Baixo/Médio/Alto/Extremo + rótulo "Sem riscos declarados") e `CtrDominios.RiscoClassificado` (os 4 do PGIA + "Não classificado"). **Erros:** `CtrClassificacaoRiscoInvalida = 810` (novo) para completude XOR, "tudo nenhuma" sem risco ("Como nenhuma das situações dos três grupos se aplica, declare ao menos um risco da contratação."), risco incompleto, e-mail inválido, mais de 20 riscos e classificação + limpar no mesmo PUT; inciso, probabilidade ou consequência fora da lista seguem `CtrDominioInvalido`. **Ciclo de vida:** POST nulo = não classificado; **PUT nulo PRESERVA** a classificação e os riscos; `LimparClassificacaoRisco` (só no UpdateDTO) apaga colunas e riscos; classificação enviada é validada inteira ANTES de qualquer escrita (disciplina do candidato), recalcula tudo, grava quem/quando e **substitui** a lista. **Idempotência do reenvio (requisito do orquestrador):** classificação IDÊNTICA à gravada — incisos de cada grupo como conjuntos ordenados, os três "nenhuma" e os riscos como **multiconjunto** de tuplas normalizadas (descrição, ação e nome com trim; e-mail com trim e minúsculas; probabilidade e consequência), sem considerar a ordem da lista — **não** regrava `risco_classificado_em/por` nem substitui os riscos (os Ids ficam): a auditoria reflete classificação de fato alterada, não o último salvamento (o `AlteradoEm/Por` do processo continua registrando o PUT); um inciso a mais já é reclassificação. **E-mail do responsável gravado SEMPRE em minúsculas** (trim + minúsculas antes da regra de e-mail do PGIA, que não mudou): com a comparação do reenvio também em minúsculas, a caixa deixa de ser informação e nenhum PUT que só troca a caixa é descartado em silêncio (achado BAIXO da revisão adversarial, reproduzido ao vivo). **Limitação conhecida (registrada, não implementada):** no reenvio idêntico as colunas derivadas (resultado, enquadramento e pontuação) **não** são recalculadas — se os pesos de `PgiaQuesitos` mudarem um dia, só uma classificação de fato alterada recalcula. **`AplicarDto` não copia a classificação de propósito:** importação e checkpoint passam por ele e não a tocam (teste com a reimportação da planilha real). **Leituras:** `RiscoClassificado` e `NivelMaximoRiscoDeclarado` planos em todas — na lista, o nível sai em **lote** (`ListarEscalasDeRiscoPorProcessosAsync`, uma consulta por página sem os textos longos; travado por teste com `DispatchProxy` contando as chamadas do repositório); `ClassificacaoRisco` aninhada só nas respostas de UM processo (GET {id}, POST, PUT **e checkpoint** — interpretação registrada: o checkpoint também é PUT de um processo e devolvê-lo sem a classificação faria a tela achar que ela sumiu). **Filtros** `RiscoClassificado`/`NivelRiscoDeclarado` como predicado EF (nível máximo = existe risco nos pares daquela cor E nenhum nos pares dos níveis acima, montado com `Expression` e constantes), fora do domínio = lista vazia; valem no export. **Painel:** `PorRiscoClassificado` e `PorNivelRiscoDeclarado`, 5 chaves sempre, quantidade desc e empate pela ordem do domínio. Fora do CSV e do PDF. Migration única **`20260915145908_CtrSupervisaoClassificacaoRisco`** (scaffold limpo, sem edição manual): `RENAME COLUMN`, 6 `ADD` em `ctr_processo`, `CREATE TABLE ctr_risco_declarado` (PK, 2 CHECKs, FK cascade), os 2 CHECKs do processo e o índice — script revisado, só objetos `ctr_*`; `has-pending-model-changes` limpo. **334 testes** (`CtrClassificacaoRiscoTest` com as 25 células e a equivalência, `CtrSupervisaoClassificacaoRiscoTest`, `CtrMigrationSupervisaoTest`).
- **Rodada dos riscos da contratação (2026-09-21) · a classificação de riscos passa a ser SÓ a lista de riscos da contratação.** Pedido do usuário: o processo não pode trazer os riscos padrão do PGIA (o questionário dos arts. 15 a 17 do Decreto nº 48.901/2026, feito para sistemas de IA); fica apenas a parte "Riscos da contratação". O que mudou:
  - **Contrato da API** (só o nosso front consome): `CtrClassificacaoRiscoDTO` ficou só com `RiscosDeclarados` (até 20); campos antigos que um cliente mande (`Q15`, `Q16Nenhuma`...) são ignorados pelo desserializador (há teste). `CtrClassificacaoRiscoResponse` ficou com `ClassificadoEm`, `ClassificadoPor` e `RiscosDeclarados`, e é **nula quando o processo não tem riscos**. Saíram `CtrProcessoResponse.RiscoClassificado`, o filtro `CtrProcessoFiltro.RiscoClassificado`, o bloco `CtrPainelResponse.PorRiscoClassificado` e `CtrDominios.RiscoClassificado`. Continuam o filtro `NivelRiscoDeclarado`, o bloco `PorNivelRiscoDeclarado` e o `NivelMaximoRiscoDeclarado` plano.
  - **Regras** (`CtrClassificacaoRisco`, ainda a fonte única): `Avaliar` valida só a lista (cada risco completo, escala completa da CGDF, no máximo 20) e **aceita lista vazia**. POST nulo ou vazio = processo sem riscos. PUT: nulo **preserva**; enviada, a lista **substitui** a gravada (vazia tira todos); `LimparClassificacaoRisco` continua valendo e equivale à lista vazia; os dois juntos seguem recusados ("não os dois"). Reenvio idêntico (`MesmosRiscos`, multiconjunto normalizado) não troca os Ids nem quem/quando. Os níveis, o nível máximo, o predicado do filtro e o painel não mudaram.
  - **Quem e quando** saem agora do risco mais recente (`CtrRiscoDeclarado.CriadoEm/CriadoPor`; a lista é substituída inteira, então os riscos de um salvamento têm o mesmo registro), e não mais de `RiscoClassificadoEm/Por`.
  - **Sem migration, de propósito** (expandir e depois contrair): as seis colunas do questionário em `ctr_processo` (`checklist_risco`, `risco_classificado`, `enquadramento_risco`, `pontuacao_risco`, `risco_classificado_em/por`) e os CHECKs `ck_ctr_processo_risco_classificado`/`ck_ctr_processo_classificacao` **continuam no banco e no modelo, sem uso** (comentados como LEGADO em `CtrProcesso` e `CtrModelConfiguration`). Nada as lê; o PUT que traz os riscos (ou o limpar) **zera as seis juntas** (`DescartarQuestionarioLegado`, o que o CHECK tudo-ou-nada aceita), mesmo no reenvio idêntico. Motivos: o deploy do PR roda antes das migrations (o CI implanta no `pull_request`), e apagar colunas com dado é irreversível; os processos classificados desde 2026-09-15 guardam o questionário ali até serem editados. `has-pending-model-changes` limpo. **Pendência registrada:** migration própria para remover as seis colunas e os dois CHECKs quando o usuário quiser.
  - **Testes:** `CtrClassificacaoRiscoTest` perdeu a equivalência com a regra do PGIA e a completude dos grupos e ganhou lista vazia, DTO sem o questionário, corpo de cliente antigo, reenvio idêntico e leitura (quem/quando do risco mais recente); `CtrSupervisaoClassificacaoRiscoTest` foi refeito para a lista, com cinco casos do legado (questionário sem riscos aparece como "Sem riscos declarados", editar outro campo não mexe nele, mexer nos riscos, reenvio idêntico e limpar zeram). 319 testes no módulo; `test-pgia` intacto (554).
- **Rodada de 2026-09-22 · assinatura conclui, criticidade pelos critérios da IN, ordem por criticidade.** Pedido do usuário (com a IN SGDI nº 01/2026 em mãos e o rascunho dos critérios): (1) a assinatura do contrato sai da "fase" e vira a data FINAL do trâmite, concluindo o processo; (2) a criticidade deixa de ser escolhida e passa a ser calculada dos critérios do art. 11, § 3º; (3) a lista sai por criticidade; (4) referências à IN com o trecho no hover (front). O que mudou aqui:
  - **Situação** (`CtrDominios.Situacao`, 8 valores): `Concluido` = `DataAssinaturaContrato != null`, com precedência sobre TUDO (inclusive restituição); a devolução ao órgão (ou dispensada com retorno ao Gab) passou a `AnaliseConcluida` ("Análise concluída"). `CalcularSituacao` ganhou o primeiro parâmetro `dataAssinaturaContrato` (sem default, como os outros flags) e todo `PredicadoSituacao` exige a assinatura nula fora de Concluído. A **Fase** (`CtrDominios.Fase`, `CalcularFase`, `PredicadoFase`, filtro `Fase`, `PorFase`, `Response.Fase`) foi REMOVIDA: era só a assinatura derivada e ficaria sem sentido. `CtrDominios.Etapa` ganhou `AssinaturaContrato` (checkpoint `PUT {id}/checkpoint`; `NaoSeAplica` continua recusado fora de UGTIC/RetornoOrgao; limpar a data reabre). A cronologia NÃO mudou: a assinatura segue fora dela (só não pode ser futura).
  - **Lista e export**: os Concluídos ficam FORA por padrão; entram com `CtrProcessoFiltro.IncluirConcluidos = true` ou com `Situacao = Concluído` (o predicado já os seleciona). **Ordem padrão**: `OrdemCriticidade` (Alta 0, Média 1, Baixa 2, nula 3, expressão traduzida em CASE) → ChegadaSgdi desc com nulos por último → Id desc; `OrderBy=Criticidade` (asc = a mesma, desc invertida) e `OrderBy=ChegadaSgdi` (desc explícito, sem a criticidade) entraram em `OrdenacoesValidas`.
  - **Criticidade derivada** (`CtrCriticidade`, fonte única; o front repete a conta só para a prévia em `src/api/contratacoes/criticidade.ts`): coluna jsonb `ctr_processo.criticidade_criterios` (`CtrProcesso.CriteriosCriticidade`, string; DTO/Response `Dictionary<string,string>`, chave = `CtrDominios.CriterioCriticidade` I..VII). Respostas: I, III, V e VII em Sim/Não; II, IV e VI em Nenhum/Baixo/Médio/Alto (`RespostasDe`, padrão = a primeira). **Regra da SGDI** (rascunho do dono, a IN não fixa fórmula): I Sim = **menos** 1 ponto (alinhado à EGD/DF é bom e baixa a criticidade; confirmado pelo usuário em 2026-09-22); III Sim = 1; II/IV/VI = 0..3 pelo grau; V Sim = 3; VII Sim = Alta direto; a soma nunca fica negativa; Baixa ≤ 2, Média 3..5, Alta ≥ 6 (constantes na classe). **Limitação registrada:** a criticidade gravada só é recalculada quando o processo é validado de novo (PUT, checkpoint, importação); mudar a regra exige regravar os processos com respostas. `ValidarProcesso` normaliza as respostas (caixa/acento, critério ausente = padrão, código ou resposta desconhecidos → `CtrDominioInvalido`) e **regrava `Criticidade` delas**; sem respostas (nulo) vale o `Criticidade` do corpo, como antes (planilha antiga e processos classificados antes da regra guardam o valor até alguém responder). `AtualizarAsync`: respostas ausentes no corpo PRESERVAM as gravadas (e a criticidade recalculada delas). `Response` traz `CriteriosCriticidade` e `PontosCriticidade` (nulo sem respostas). `ValidarCriticidadeNaoRemovida` continua valendo.
  - **Painel**: `PorFase` saiu; entraram `TotalConcluidos`, `Concluidos` (até 50, assinatura mais recente primeiro, mesmos dados da lista) e `TemposMedios.ChegadaParaAssinatura`; gargalos ignoram Concluído, Análise concluída e Restituído; `PorSituacao` tem 8 chaves.
  - **CSV** (29 colunas): as 7 dos critérios no fim (`Critério I - ...` a `Critério VII - ...`, `CtrCsv.ColunasCriterios`), como BLOCO em `CtrCsvColunasOpcionais.CriteriosCriticidade` (ausente preserva; presente e todo vazio = respostas nulas e vale a coluna Criticidade; qualquer célula preenchida avalia o bloco, as vazias no padrão). Resposta fora do domínio rejeita nomeando a coluna; **Criticidade digitada que não confere com os critérios rejeita a linha** (a coluna é derivada com respostas; divergência só nasce de edição à mão). Export escreve as respostas (vazias sem elas).
  - **Migration** `20260922140501_CtrCriticidadeCriteriosAssinaturaConclui` (scaffold limpo): só `AddColumn criticidade_criterios jsonb` em `ctr_processo`; sem backfill (não há como inferir respostas de um valor). Testes: `CtrCriticidadeConclusaoTest` (regra, normalização, PUT preservando, ordem, concluídos fora/dentro, checkpoint da assinatura, CSV e a inspeção da migration) + ajustes em `CtrRodadaChefiaTest` (fase → conclusão), `CtrPainelTest` (8 situações) e nos que esperavam "Concluído" na devolução. **352 testes**.
- **Categoria "Serviços de TIC"** (pedido de 2026-09-22): 13º valor de `CtrDominios.CategoriaObjeto` (antes de "Sem objeto / Indefinido"); o CHECK é reproduzido do domínio, então a migration `20260922175843_CtrCategoriaServicosTic` só o recria. Importação e export aceitam o valor por tabela (`ResolverCategoria`).
- **Dados da contratação e critério IV** (segundo pedido de 2026-09-22): `ctr_processo` ganhou `valor_estimado numeric(18,2)`, `hospedagem_cetic varchar(30)` (CHECK `ck_ctr_processo_hospedagem_cetic` gerado de `CtrDominios.HospedagemCetic`: Sim, Não, Parcialmente, Não aplicável (SaaS)) e `usa_gdfnet boolean`, todos nullable (nulo = não informado), com `ValorEstimado`, `HospedagemCetic` e `UsaGdfnet` nos DTOs e na response. `ValidarProcesso` recusa valor negativo ou acima de 9999999999999999,99 (`CtrProcessoInvalido`), arredonda para centavos e grava a hospedagem na grafia do domínio (`CtrCsv.ResolverHospedagemCetic`, sem caixa nem acento; fora dele, `CtrDominioInvalido`). No PUT o enviado vale e nulo limpa (o formulário manda os três); o checkpoint não os toca. **CSV com 32 colunas**: `Valor estimado (R$)`, `Hospedagem no CeTIC-DF` e `Usa a rede GDFNet` depois dos critérios, cada uma independente em `CtrCsvColunasOpcionais` (ausente preserva, presente e vazia limpa, inválida rejeita nomeando a coluna), sem a proteção contra fórmula. O valor aceita `R$`, espaços, `1.234.567,89`, inteiro e ponto decimal fora do desenho do milhar (`1234.5`; `1.500` é milhar) e sai `1234567,89`; negativo o parser rejeita, o limite fica com a validação. **Critério IV** ganhou "Não foi possível avaliar com as informações apresentadas" (a última da lista, vale 0 como Nenhum; o atalho "Não foi possível avaliar" é aceito na API e na planilha e gravado como a frase completa, via `CtrCriticidade.ResolverResposta`); II e VI seguem com quatro respostas. Migration `20260922190259_CtrValorHospedagemGdfnet` (scaffold limpo, só `ctr_processo`). 459 testes (`CtrValorHospedagemGdfnetTest`).
- **Comunicação do TCDF** (pedido de 2026-09-23 da equipe da SGDI: a "nova manifestação" abria o cadastro do processo da contratação, com critérios de criticidade que a comunicação do TCDF não permite avaliar, porque ela não traz o processo da contratação). Front: seção de mesmo nome no `CLAUDE.md` do SGDP-Angular.
  - **Dados na manifestação**: `ctr_manifestacao_tcdf` ganhou `processo_comunicacao_tcdf varchar(25)` (processo SEI em que o TCDF comunicou a SGDI, no formato SEI, validado com o `CtrProcessoService.FormatoSei`, agora `internal`), `data_recebimento date` (não futura e não anterior à data do ofício), `ato_tcdf varchar(20)` (`CtrDominios.AtoTcdf`: Despacho Singular ou Decisão; CHECK `ck_ctr_manifestacao_ato_tcdf`) e `numero_ato_tcdf varchar(60)`, tudo ou nada (`ck_ctr_manifestacao_comunicacao`). A data do ofício continua (o despacho a cita) e os "dias para regularização" seguem sendo o prazo do inciso II (art. 40, parágrafo único, da IN: 5 dias úteis, valor sugerido pelo front).
  - **Obrigatórios na criação** (as duas rotas de POST). Na edição, `ValidarManifestacao` recebe `exigirComunicacao` = `TemComunicacao(gravada)`: quem já tem os quatro não pode apagá-los; as registradas antes deles (os quatro nulos, sem backfill) podem seguir sem eles ou recebê-los todos (preencher um pede os demais).
  - **`POST api/contratacoes/manifestacao`** (`CtrComunicacaoTcdfCreateDTO`, gate `CanEdit(Manifestacao)`): vem exatamente um de `ProcessoId` (processo já cadastrado, 404 no controller quando não existe ou foi excluído; é o caminho do inciso I, que reporta a criticidade dele) ou `Contratacao` (`CtrContratacaoTcdfDTO`: `OrgaoNome`, `OrgaoSigla`, `Objeto`, `CategoriaObjeto`, `ValorEstimado`). A contratação nova vira processo de origem TCDF com `NumeroProcesso` = processo de comunicação (o único processo SEI que a SGDI tem dela), sem trâmite nem critérios, validado pelo `ValidarProcesso` e gravado com a manifestação num `SaveChanges` só. Recusas: os dois ou nenhum; contratação nova no inciso I ("registre a comunicação no processo dela"); número que já é de processo ativo (`CtrProcessoDuplicado`, "registre a comunicação nele"). `POST processo/{id}/manifestacao` continua, com as mesmas regras.
  - **Despacho em PDF**: o "Processo SEI nº" do cabeçalho passou a ser o processo de comunicação, em que o despacho é juntado (`CtrDespachoPdf.NumeroDoProcessoSei`; sem ele, o número do processo, como antes), e o nome do arquivo segue o mesmo número. O texto do despacho não mudou (o ato e a data de recebimento não entram).
  - **Leituras**: a response traz os quatro campos e os espelhos `CategoriaObjeto`, `ValorEstimado` e `Origem` do processo; a busca da lista procura também no processo de comunicação (com teste de nulo antes do `ToLower`, por causa do InMemory). A mensagem de órgão vazio do `ValidarProcesso` ficou neutra ("Informe o nome do órgão."), porque vale também para o órgão auditado.
  - Migration `20260923184153_CtrComunicacaoTcdf` (scaffold limpo: 4 `AddColumn` nullable e 2 CHECKs, só `ctr_manifestacao_tcdf`). 493 testes (`CtrComunicacaoTcdfTest`; os helpers de manifestação do `CtrTestBase` passaram a trazer os quatro dados, com a data de recebimento de hoje, que vale para qualquer data de ofício que o teste escolha).
- **Estado:** implementado (fundação e CRUD + painel/export + importação + manifestação ao TCDF + rodada da chefia + esclarecimentos/pendências + supervisão contínua/classificação de riscos + riscos da contratação sem o questionário do PGIA + assinatura conclui/criticidade pelos critérios + comunicação do TCDF), aguardando revisão do usuário.

## Módulo Governança Estratégica (planejamento)

Terceiro módulo aditivo do repositório: a Política de Governança de TIC do Decreto nº 48.900/2026 (princípios do art. 4º, PETIC-DF e o PDTIC de cada órgão, passo a passo). Plano aprovado: `../plano_governanca_estrategica.md` (manda em caso de dúvida). Uma entrega por PR (back-end primeiro, com merge logo depois; depois o front); esta seção cresce a cada entrega. Front: seção de mesmo nome no `CLAUDE.md` do SGDP-Angular.

**Nomes fixos:** id `planejamento` (card "Governança Estratégica"), rotas `api/planejamento/...`, política `ModulosSgdp.PoliticaPlanejamento` (`modulo:planejamento`) em todo controller do módulo, tabelas `pe_` (constraints `pk_pe_*`, `ck_pe_*`, `ux_pe_*`, `ix_pe_*`, `fk_pe_*`), classes `Pe*`, namespaces `Models.Planejamento`, `api.Planejamento`, `service.Planejamento` (interfaces em `service.Interface`) e `Controllers.Planejamento`. Erros na faixa **1000 a 1099** do `ErrorCode`, respondidos como `{ Code, Message }` com 400, 403, 404 ou 409 (padrão do `AcessoController`, nunca o 500 sem corpo dos módulos antigos). Migrations com prefixo `Pe`, uma por entrega, com script revisado (só objetos `pe_*`, fora os dois CHECKs da E1).

### E1 · Base e acesso (2026-09-24)

- **Papéis** (`app/Auth/PapeisPlanejamento.cs`): `pe_admin` (administrador do módulo: dá e tira papéis, inclusive o de administrador, e decide os pedidos do módulo), `pe_sgdi` (equipe da SGDI), `pe_cgtic` (Secretaria Executiva do CGTIC, art. 9º), `pe_orgao` (equipe do órgão) e `pe_orgao_consulta` (consulta do órgão). `EhValido` (nulo **não** é válido, ao contrário de `PapeisPgia`: no módulo não existe acesso sem papel), `EhDeOrgao` (os dois de órgão) e `EhGlobal` (admin, sgdi, cgtic). O perfil admin do SGDP tem tudo sem papel.
- **Tabelas** (`app/Models/Planejamento/`, mapeamento em `PeModelConfiguration`, domínios em `PeDominios`): `pe_papel_usuario` (PK = `user_id`, FK cascade para `Users`; `papel` varchar(30) com `ck_pe_papel_usuario_papel`; `concedido_em/por` da primeira concessão; `alterado_em/por` da última troca) e `pe_papel_usuario_historico` (`id` identity; `papel_anterior` e `papel_novo` nuláveis, nulo = sem papel, sem CHECK de propósito para guardar o valor da época; `origem` com `ck_pe_papel_usuario_historico_origem`: `pessoas`, `pedido`, `gestao_acessos`, `modo_local`; `pedido_acesso_id` sem FK; `alterado_em/por`; índice `ix_pe_papel_usuario_historico_user (user_id, alterado_em)`). **`Users` e `pedido_acesso` não ganharam coluna.**
- **Migration `20260924151108_PeAcessoPapeis`** (script revisado): `CREATE TABLE pe_papel_usuario` e `pe_papel_usuario_historico` (PK, CHECK e FK cascade cada), `CREATE INDEX ix_pe_papel_usuario_historico_user` e a troca de `ck_acesso_modulo_modulo` e `ck_pedido_acesso_modulo` (DROP e ADD com `planejamento`, como a `20260922175843_CtrCategoriaServicosTic` fez). Nada mais. O Down volta os CHECKs antigos (falha se já existir linha com `planejamento`, como todo CHECK recriado). O `PeMigrationTest` trava o escopo por inspeção das operações.
- **Acesso e papel andam juntos, sempre pelo `AcessoModuloService`:** `planejamento` entrou em `ModulosSgdp.Todos`, `Concedidos` e `Liberaveis`; o acesso é a linha de `acesso_modulo` (origem `sistema`) e `CalcularModulos` só olha a concessão. A escrita tem um ponto só, `PrepararPapelPlanejamento` (privado, não grava): papel igual ao atual não grava nada; mudança grava uma linha de histórico; papel nulo tira papel e concessão. Quem usa: `DefinirPapelPlanejamentoAsync` (tela do módulo e modo local; dar o papel encerra o pedido pendente do módulo como aprovado), `PrepararLiberacaoAsync` (aprovação de pedido, origem `pedido` com o id do pedido) e `DefinirAcessosAsync` (gestão de acessos). `DefinirConcessaoAsync` recusa `planejamento` (não existe concessão sem papel).
- **Regra do deploy (não quebrar):** o PR publica o código antes da migration, que só roda no merge. O caminho de cada requisição (`ModuloAcessoClaimsTransformation`, `CalcularModulos`, `ModulosDoPrincipalAsync`, `ModulosDoUsuarioAsync` e o `GET /api/Auth/me`) **nunca** lê tabela `pe_`. As demais leituras têm guarda: a gestão de acessos só lê o papel de quem tem a concessão do módulo na página, a fila de pedidos só quando a página tem pedido do módulo e o `DecisorAsync` só para quem tem a claim do módulo e não é admin. Antes da migration nenhuma dessas linhas pode existir (os CHECKs antigos recusam `planejamento`), então a Administração e os pedidos seguem de pé no intervalo; só as telas do módulo novo falham. O teste `AcessoModuloTest.IntervaloDoDeploy_SemAsTabelasPe_OCaminhoDeCadaRequisicaoContinuaDePe` roda esse caminho num `AppDbContext` sem as entidades `pe_` (qualquer leitura lança), e uma mutação da guarda do `DecisorAsync` o derruba.
- **Endpoints existentes (só campos opcionais; ausente não muda nada):**
  - `POST api/pedidos-acesso` aceita `planejamento`. `POST {id}/aprovar`: `PedidoAcessoAprovarDTO.PapelPlanejamento` é obrigatório e válido nos pedidos do módulo (senão 400 `PedidoAcessoInvalido`, com mensagem) e proibido nos outros (400); aprovar grava concessão, papel e histórico (origem `pedido`) no mesmo `SaveChanges` da decisão. `PedidoAcessoResponse.PapelPlanejamento` é o papel de **hoje** da pessoa (só nos pedidos do módulo, lido em lote).
  - Quem decide: admin (todos os liberáveis), `pgia_sgdi` (PGIA), `pe_admin` (planejamento); quem tem os dois papéis decide os dois. `GET pedidos-acesso`, `GET pendentes`, aprovar e recusar respeitam a lista.
  - `GET api/acessos/usuarios` e `usuario/{id}`: `UsuarioAcessoResponse.PapelPlanejamento` (uma consulta por página). Filtro `Modulo=planejamento` = concessão do módulo ou retrato de admin.
  - `PUT api/acessos/usuario/{id}`: `Planejamento` (bool?) e `PapelPlanejamento` (string?). Nulo não muda nada (front antigo não tira acesso de ninguém); `true` exige papel válido (o enviado ou o atual; sem nenhum, 400 `AcessoInvalido`); `false` tira acesso e papel; papel sem `Planejamento = true` é 400. Grava histórico (`gestao_acessos`) e encerra o pedido pendente.
  - `GET /api/Auth/me`: só `Modulos` passa a poder conter `planejamento`.
- **API do módulo** (`Controllers/Planejamento/PePessoasController.cs`, `api/planejamento`, JSON PascalCase):
  - `GET meu-papel` (qualquer pessoa com o módulo): `PeMeuPapelResponse { Papel, EhAdminGeral, OrgaoId, OrgaoNome, OrgaoSigla, UnidadeNome }`.
  - `GET pessoas?Filtro=&Papel=&Page=&PageSize=` (pe_admin e admin geral): `PagedResponse<PePessoaResponse>` com `UserId, Nome, Email, UnidadeNome, OrgaoId, OrgaoSigla, OrgaoNome, Papel, ConcedidoEm, ConcedidoPor, EhAdminGeral`; só quem tem papel, em ordem de nome; papel fora do domínio devolve lista vazia; página saneada (20 padrão, 100 no máximo). `EhAdminGeral` vem do retrato do último login.
  - `GET pessoas/candidatas?Filtro=` (idem): até 20 `PeCandidataResponse { UserId, Nome, Email, UnidadeNome, OrgaoSigla }`, só quem já entrou no SGDP (`KeycloakId` preenchido) e não tem papel; menos de 3 letras devolve lista vazia.
  - `PUT pessoas/{userId}/papel` com `{ Papel }` (idem): cria, troca ou, com nulo, tira. Devolve **200 com o `PePessoaResponse`**; ao tirar, `Papel`, `ConcedidoEm` e `ConcedidoPor` voltam nulos e os demais campos preenchidos. Papel de órgão para quem não tem órgão grava assim mesmo e volta com `OrgaoId` nulo.
  - Erros: `PeUsuarioNaoEncontrado = 1000` (404; também quando o cadastro de quem chama não existe), `PePapelInvalido = 1001` (400), `PeSemPermissao = 1002` (403 com corpo), `PeAutoRebaixamento = 1003` (409: `pe_admin` tirando ou trocando o próprio papel; o admin geral pode tudo).
- **Autorização** (`service/Planejamento/PePermissionService.cs`, ponto único que as próximas entregas estendem com recurso + ação + órgão): `GetContextAsync(principal)` monta o `PeUserContext` (UserId, Email, EhAdminGeral, Papel, unidade e órgão) sob demanda nas actions, achando a pessoa pelo sub e depois pelo e-mail, como o `GetOrCreateUserAsync`. Órgão = `pgia_orgao` **ativo** da unidade (a mesma regra do `PgiaPermissionService`, que não foi tocado), em lote por `OrgaosPorUnidadeAsync`. `PodeGerirPessoas` (pe_admin e admin geral), `VeTodosOsOrgaos` (admin geral e papéis globais), `EhDoOrgao(orgaoId)` (papel de órgão no próprio órgão). O `PePessoaService` lê direto do `AppDbContext` e grava só pelo `IAcessoModuloService`.
- **Modo local:** `LoginLocalRequest.PapelPlanejamento` (validado) grava papel, concessão e histórico (`modo_local`, autor `modo-local`) pelo `DefinirPapelPlanejamentoAsync`; ausente não mexe. Papel de órgão põe a pessoa na unidade `SES` com o órgão SES ("Secretaria de Estado de Saúde", criado pelo `CriarOrgaoAsync` real, com os prazos do PGIA), a não ser que a persona já vá para o Órgão de Teste do PGIA; papel global usa a unidade central de teste. `ConcederModulos` com `planejamento` e sem `PapelPlanejamento` só vale para quem já tem papel (senão 400). Personas no `../EXECUTAR-LOCAL.md`.
- **Limitações registradas:** dois administradores gravando o papel da mesma pessoa no mesmo instante: vale a última gravação (como no `PapelPgia`), e a primeira concessão simultânea pode cair na PK e voltar 500, como as concessões da gestão de acessos. A migration não tem backfill (ninguém tem o módulo antes dela).
- **Testes:** `test-planejamento/` (xUnit + EF InMemory; `PeTestBase` com SES e SEEC, unidade central sem órgão, uma pessoa por papel, admin geral sem papel, pessoa sem papel e pessoa sem unidade): **90 testes** (`PePermissionServiceTest`, `PePessoaServiceTest`, `PePessoasControllerTest` com as rotas do contrato, `PeModoLocalTest` e `PeMigrationTest`). O `test-pgia` ganhou 34 (mapa de controllers com o `PePessoasController`, `CalcularModulos`, política, `DefinirAcessosAsync` com os campos opcionais, filtro, `DefinirConcessaoAsync`, intervalo do deploy, pedir, aprovar com e sem papel, quem decide e a fila): 588 no total. `test-contratacoes` intacto (493).

### E2 · Modelo configurável e níveis de maturidade (2026-09-24)

O administrador do módulo (e o admin geral) configura o que cada nível de maturidade pede (passos, seções e campos), o nível de cada órgão e ajustes por órgão; a trilha do órgão passa a vir do back-end. Os dados que o órgão preenche (registros) chegam na E3 e na E4.

- **Tabelas** (migration **`20260924162542_PeModeloConfiguravel`**, script revisado: só as 13 tabelas `pe_` e 17 índices; as FKs para `pgia_orgao` ficam nas tabelas novas, com RESTRICT; nenhuma tabela existente tocada; o `PeMigrationModeloTest` trava o escopo): `pe_nivel` (código único e estável), `pe_etapa`, `pe_passo` e `pe_passo_nivel`, `pe_secao` e `pe_secao_nivel`, `pe_campo` e `pe_campo_nivel` (chave única na seção, inclusive entre os apagados, porque é a chave do valor no jsonb dos registros), `pe_opcao` (valor único no campo e estável), `pe_orgao_config` (PK = órgão; nível escolhido com justificativa), `pe_orgao_ajuste` (um por órgão e item, sem FK no alvo), `pe_configuracao` (chave e valor jsonb) e `pe_modelo_historico` (antes e depois em jsonb, índices por item e por data). Sem linha em `pe_*_nivel` = desligado. Entidades em `app/Models/Planejamento/` (interfaces `IPeAuditavel`, `IPeOrdenavel` e `IPeSituacaoNivel` em `PeContratos.cs`), mapeamento em `PeModelConfiguration.ApplyPeModeloConfiguration`, domínios em `PeDominios` (situação, escopo, tipo de seção, alvo do ajuste, tipo de passo, tipo de campo, cálculo, catálogo, largura, cor, tipo de arquivo, incisos, entidade e ação do histórico). CHECKs: os domínios, o formato das chaves (regex), incisos "I" a "IX" (vários com vírgula: "V,VI,IX") e o par escopo `pdtic` com passo e sem situação geral (`petic` e `df` ao contrário). Booleanos sem default no banco (o EF trocaria um `false` explícito pelo default `true`).
- **Além da tabela da spec:** `pe_campo.travado` (o tema das ações) e `pe_opcao.travada` (os três temas do decreto), que a spec pedia travados mas sem coluna. Nas respostas: `Travado` no campo (travado ou principal de seção travada), `Travada` na opção, `EtapaId`, `PassoId`, `SecaoId` e `CampoId` nos itens, `Orgaos` no nível (órgãos com ele escolhido), `Id` na etapa da trilha e `NivelAtivo` na trilha.
- **Regra do deploy (mantida):** nada no caminho de cada requisição lê as tabelas novas. O carregador roda num `BackgroundService` (`PeCarregadorModeloHostedService`), fora das requisições e sem segurar o boot; sem as tabelas, registra um aviso no log e tenta de novo (30 s, 1 min, 2 min, 5 min e depois a cada 10 min) até carregar, o que cobre o PR (código antes da migration) e o merge (a imagem nova pode subir antes de a migration terminar). Enquanto a tabela não existe, as actions do módulo respondem **409 `PeModeloIndisponivel` (1024) com corpo** (PostgresException 42P01, `PeBanco.TabelaAusente`), nunca 500. Conferido num PostgreSQL de verdade (banco só com as migrations até a E1): API de pé, `/me` e a E1 normais, modelo e órgãos em 409 com corpo.
- **Carregador** (`PeCarregadorModelo`): conteúdo em `app/Models/Planejamento/Seed/modelo-inicial.json`, embutido no assembly (`LogicalName` `Planejamento.modelo-inicial.json`; fora do Content da publicação). A versão fica em `pe_configuracao.seed_modelo_versao`: versão já carregada = uma leitura e nada mais; versão nova = insere só o que falta, achando cada item pela chave (nível pelo código, etapa, passo e seção pela chave, campo pela chave na seção, opção pelo valor no campo), e **nunca atualiza item existente** (nem texto, nem situação, nem ordem), então o que o administrador mudou fica. Item do sistema nunca é apagado de verdade, então não volta por engano. Uma transação só, com `pg_advisory_xact_lock` (duas réplicas subindo juntas não carregam em dobro); o config de cada campo novo passa pelo `PeConfigCampo` com o modelo inteiro em volta; JSON inválido (chave repetida, travado desligado, seção sem exatamente um campo principal, config errado) não grava nada e aparece no log. **Para mudar o conteúdo, edite o JSON e suba a `versao`**: o que for novo entra; o que já existe não muda nem se o texto no JSON mudar (para isso, uma migration de dados). Versão 1: 3 níveis (`basico`, `intermediario`, `avancado`), 7 etapas, **50 passos** (10, 12, 13, 6, 2, 3 e 4 por etapa), **68 seções**, **345 campos**, **262 opções** e `periodicidade_monitoramento_padrao` = "trimestral".
- **Decisões do conteúdo** (seções 7 e 8 do plano):
  - Chave do passo = `etapa.código`, com os códigos da trilha estática da E1 do front (`preparacao.abrangencia`, `planejamento.metas-acoes`); seções e campos com sublinhado. Títulos exatamente os da seção 7; "o que fazer" em uma ou duas frases; base legal no formato das dicas.
  - Situação do passo em cada nível: "sim" e "trav." = obrigatório, vazio = desligado; "lista", "padrão", "objetivos do PETIC", "art. 4º marcado", "critério simples", "só a situação das ações" e "simples" = obrigatório, simplificado pelos campos (os "Básico: ..." da seção 8 viram os campos ligados no Básico). Seção de passo desligado num nível repete a situação do nível seguinte, para o passo religado já vir pronto.
  - Travados: os passos 2.3 (I), 2.4 (II), 2.9 (III), 2.10 (VIII), 3.3 (VII), 3.4 (V, VI e IX, tipo `conferencia_temas`) e 3.5 (IV), e as seções `diagnostico_ambiente`, `ativos`, `necessidades`, `aquisicoes_ia`, `metas`, `acoes` e `contratacoes`. O campo `acoes.tema` (lista múltipla) é travado e os três temas do decreto são opções travadas. `aquisicoes_ia` é travada mas opcional (sem aquisição planejada, fica vazia; os sistemas em uso vêm do PGIA na E4).
  - Prioridade (combinado com o front): `necessidades.gravidade`, `urgencia` e `tendencia` são `numero` de 1 a 5 (`{ min: 1, max: 5, casas: 0 }`) e `necessidades.prioridade` é `calculado` `produto` delas; o front acha a prioridade pelo cálculo `produto` ou `soma_ponderada`. No Básico, `prioridade_simples` (alta, média, baixa). Nível de risco: `riscos.nivel` é `calculado` `nivel_risco` com a `matriz` probabilidade (baixa, média, alta) x impacto (baixo, médio, alto) e as opções baixo, médio e alto (com cor) no próprio campo; no Básico, `nivel_simples`.
  - SWOT em quatro tabelas (`swot_forcas` F, `swot_fraquezas` D, `swot_oportunidades` O, `swot_ameacas` AM, porque A é o prefixo das ações), para as ligações fraqueza com necessidade e ameaça com risco que os avisos da E4 usam. Prefixos: DR, E, PD, OT, AT, F, D, O, AM, NI, N, IA, M, A, C, FCS, R, P, IM, IR.
  - Aprovações (1.10, 2.12, 3.11, 4.6, 6.3, 7.3 e 7.4): uma seção formulário por passo (decisão, data, instância, tipo e número do ato, SEI, documento e observação); a 6.3 decide "seguir" ou "revisar". Os passos 3.2 (priorização) e 2.11 (consolidação) têm só uma seção de resumo opcional: as notas e as ligações ficam na tabela `necessidades` (a E4 decide se a tela do passo mostra essas colunas). O 3.4 tem a seção opcional `temas_sem_acao`, com uma justificativa por inciso, pronta para a E4.
- **Resolução da trilha** (`PeTrilhaResolver`, sobre o `PeModeloDados`, que lê o modelo inteiro de uma vez): para cada item, o ajuste do órgão, senão a situação no nível do órgão, senão desligado; travado (e o principal de seção travada) nunca desliga, vira obrigatório, nem com ajuste gravado por fora nem em nível sem linhas. Etapa sem passo visível some; seção só aparece se o passo aparece; campo, se a seção aparece; **campo `ligacao_secao` some quando a seção que ele liga não aparece** para o órgão. Numeração pela posição entre os visíveis (N e N.M). Órgão sem nível usa o padrão (o primeiro ativo pela ordem, `NivelPadrao: true`); órgão num nível desativado continua nele (`NivelAtivo: false`). No modelo inicial: Básico 23 passos em 6 etapas (a avaliação intermediária some), Intermediário 40, Avançado 50 (numeração igual à do plano).
- **API** (JSON PascalCase; política `modulo:planejamento`; erros `{ Code, Message }` por `PeRespostas`, que também converte o `DbUpdateException` de duas gravações simultâneas em 409 `PeConflitoGravacao`):
  - `PeModeloController` (`api/planejamento/modelo`): `GET` (qualquer papel e admin geral; `?incluirExcluidos=true` só vale para quem altera, os outros recebem sem os apagados), `GET trilha?orgaoId=` (papel de órgão: o próprio, sem `orgaoId` = o dele, outro órgão 403, sem órgão 404; papéis globais e admin: qualquer órgão, e sem `orgaoId` 400 `PeOrgaoObrigatorio` quando a pessoa não tem órgão), `GET historico?entidade=&entidadeId=&page=&pageSize=` (papéis globais e admin; `PagedResponse`, do mais novo; entidade fora do domínio = lista vazia). Escrita (pe_admin e admin geral; os outros recebem 403): `POST niveis` (`Nome`, `Descricao`, `Codigo` opcional e normalizado: "nivel-piloto" vira "nivel_piloto"; `CopiarDe` opcional), `PUT niveis/{id}` (`Nome`, `Descricao`, `Ativo`), `PUT niveis/ordem`, `PUT etapas/{id}` (`Titulo`, `Descricao`, `ReferenciaGuia`, `Ordem` = posição; a ordem atual reenviada não mexe), `POST passos` (tipo `dados`), `PUT passos/{id}`, `PUT passos/{id}/niveis`, `DELETE passos/{id}`, `PUT passos/ordem`, e o mesmo para `secoes` e `campos`, `POST campos/{id}/opcoes`, `PUT campos/{id}/opcoes/ordem` (a mais que a spec), `PUT opcoes/{id}` (`Rotulo`, `Cor`, `Ativa`; o valor não muda) e `DELETE opcoes/{id}`. POST devolve **201** com o item; PUT e DELETE de passo, seção e campo devolvem **200** com o item na forma do `GET modelo` (DELETE: exclusão lógica, `Excluido: true`, idempotente); `DELETE opcoes` devolve **204** quando apaga (criada pelo administrador e sem uso) ou **200** com a opção desativada (do sistema ou em uso). `PUT .../ordem` devolve a etapa (passos), a lista das seções na nova ordem (seções), a seção (campos) ou o campo (opções).
  - `PeOrgaosController` (`api/planejamento/orgaos`): `GET ?Filtro=` (papéis globais e admin; órgãos ativos por sigla, com o nível escolhido ou o padrão, `NivelId` preenchido nos dois casos), `PUT {id}/nivel` (pe_admin e admin geral; justificativa obrigatória; nível inativo 409), `GET {id}/nivel/historico` e `GET {id}/ajustes` (quem vê o órgão: globais, admin e o papel de órgão no próprio), `PUT {id}/ajustes` (pe_admin e admin geral; **lista inteira**: o que não vem, ou vem com `Situacao: null`, sai; só itens do PDTIC, não apagados; travado não desliga).
- **PUT parcial** (`PeCorpoParcial`): o controller lê o corpo como `JsonElement` e marca as propriedades que vieram. Ausente = não muda; presente com nulo ou texto vazio = limpa o opcional e é recusado (400) no obrigatório (título, rótulo, nome, o que fazer). Nomes sem diferenciar maiúsculas. `PUT .../niveis` aceita o mapa com todos os níveis ou só alguns (os ausentes não mudam); fora do PDTIC, `{ SituacaoGeral }`.
- **Regras** (erros 1010 a 1025 no `ErrorCode`): `PeItemNaoEncontrado` 1010 (404), `PeOrgaoNaoEncontrado` 1011 (404), `PeDadosInvalidos` 1012 (400; também pai inexistente informado no corpo), `PeItemTravado` 1013 (409: travado não desliga; opção travada não desativa nem apaga; passo travado não aceita "não se aplica"), `PeItemDoSistema` 1014 (409: tipo e chave de item do sistema não mudam, e ele não é apagado; mandar o mesmo tipo passa), `PeChaveDuplicada` 1015 (409), `PeConfigInvalida` 1016 (400), `PeOrdemInvalida` 1017 (400: a lista tem todos os irmãos, uma vez cada), `PeNivelInativo` 1018 (409), `PeUltimoNivelAtivo` 1019 (409), `PeJustificativaObrigatoria` 1020 (400), `PeOrgaoObrigatorio` 1021 (400), `PeItemExcluido` 1022 (409: item apagado, ou filho de item apagado, não é editado), `PeItemEmUso` 1023 (409: campo usado num cálculo não é apagado nem muda de chave ou de tipo; seção alvo de uma ligação não é apagada, não muda de chave nem deixa de ser tabela), `PeModeloIndisponivel` 1024 (409), `PeConflitoGravacao` 1025 (409). Criação: passo nasce opcional no nível ativo mais alto e desligado nos outros; seção e campo nascem desligados (fora do PDTIC, situação geral desligada); chave gerada do título sem acento (`_2`, `_3` quando repete). Nível novo em branco já vem com os travados obrigatórios; com `CopiarDe`, copia as situações. Opção de campo que entra num cálculo numérico precisa de valor numérico.
- **Config por tipo** (`PeConfigCampo`, estrito: chave desconhecida = 400, e chave com nulo conta como ausente; saída normalizada): `texto_curto { max }` (1 a 1000), `texto_longo { max }` (1 a 50000), `numero { min, max, casas (0 a 6), unidade }`, `ligacao_secao { secao, multipla }` (tabela do mesmo escopo, não apagada), `ligacao_catalogo { catalogo, multipla }` (`petic_objetivo`, `petic_eixo`, `principio`, `pgia_sistema`), `arquivo { tipos (pdf, png, jpg), maxMb (até 25) }`, `calculado { calculo, campos, pesos, matriz }` (`produto` com 2 ou mais campos, `soma_ponderada` com peso maior que zero para cada campo, `subtracao` com 2, `nivel_risco` com 2 listas e a matriz completa das opções ativas; só campos da mesma seção, de número, moeda, percentual ou lista com valores numéricos; calculado não usa outro); os demais tipos não têm config (`{}`). Pesos e matriz fora do cálculo deles são descartados. No PostgreSQL o jsonb reordena as chaves do config: leia por nome.
- **Histórico:** toda criação, alteração, mudança de situação, de ordem, exclusão e remoção grava uma linha com antes e depois (JSON com os acentos legíveis); alteração sem mudança de fato não grava. Nível e ajustes de órgão entram como `orgao_nivel` e `orgao_ajuste` (`entidade_id` = órgão). O carregador não grava histórico.
- **Testes:** `test-planejamento` com **260** (90 da E1 e 170 novos: `PeCarregadorModeloTest`, com a tabela da seção 7 inteira, `PeTrilhaTest`, `PeModeloServiceTest`, `PeOrgaosTest`, `PeModeloControllerTest` e `PeMigrationModeloTest`; base `PeModeloTestBase`, que carrega o modelo inicial com o carregador real). O `test-pgia` continua com 588: o mapa de controllers ganhou `PeModeloController` e `PeOrgaosController`, e o contexto do intervalo do deploy passou a tirar todas as entidades de `Models.Planejamento`. `test-contratacoes` intacto (493).
- **Limitações registradas:** "em uso" de uma opção só olha a matriz do nível de risco (a E3 acrescenta os registros no `OpcaoEmUsoAsync`); chave e tipo de campo e de seção criados pelo administrador ainda mudam (não há registros na E2; a E3 precisa bloquear quando houver); o validador do config é estrito, então chave nova de config pede mudança no `PeConfigCampo`; "não se aplica" e a situação de cada passo são da E4.

### E3 · Referenciais, registros, deliberações e planilhas (2026-09-24)

Nasce o motor que grava os dados descritos pelo modelo (registros). Os primeiros donos são o catálogo do DF (princípios do art. 4º do Decreto nº 48.900/2026 e diretrizes do ciclo, art. 9º, VIII, do Decreto nº 48.899/2026) e as versões do PETIC-DF (art. 11 do Decreto nº 48.900/2026), com envio ao CGTIC e deliberação pela Secretaria Executiva. Nasce também a exportação em CSV e XLSX. A E4 reaproveita tudo para o PDTIC de cada órgão.

- **Tabelas** (migration **`20260924174627_PeReferenciaisRegistros`**, script revisado: só `CREATE TABLE` das 6 tabelas `pe_` e 12 índices; as FKs só apontam para tabelas `pe_`; nenhuma tabela existente tocada; o `PeMigrationReferenciaisTest` trava o escopo):
  - `pe_petic`: versão (`"N.0"`, dada pelo servidor: a primeira é 1.0 e cada nova é o maior número mais um; única), título, vigência (`DateOnly`, fim não antes do início), situação (`rascunho`, `em_deliberacao`, `aprovado`, `substituido`; **token de concorrência**), `anterior_id` (a vigente quando a versão nasceu, FK RESTRICT) e `aprovado_em` (CHECK: preenchido só em aprovado e substituído). **`ux_pe_petic_situacao`**, único parcial em `situacao`: no máximo uma versão em rascunho, uma em deliberação e uma aprovada (a vigente).
  - `pe_registro`: seção (FK RESTRICT), `petic_id` (nulável; nulo = catálogo do DF; FK CASCADE), código (CHECK `^[A-Z][A-Z0-9]*[0-9]$`), ordem, `dados` jsonb, `sistema`, auditoria. `ux_pe_registro_petic_codigo` (petic_id, secao_id, codigo) e `ix_pe_registro_secao` (secao_id, ordem).
  - `pe_vinculo`: origem, campo e destino (único); origem CASCADE; **destino NO ACTION** (recusa apagar o destino ligado como o RESTRICT, mas confere no fim do comando, para a exclusão em cascata de um rascunho não falhar pela ordem das linhas); CHECK origem diferente do destino; índices por destino (quem liga) e por campo.
  - `pe_registro_sequencia` (**a mais que a spec**): PK (secao_id, dono), com dono `"df"` ou `"petic:12"` (a E4 usa `"pdtic:ID"`), e `ultimo` como **token de concorrência**. É o que impede reaproveitar o código de um registro apagado e o que faz duas inclusões ao mesmo tempo não darem o mesmo código (a segunda recebe 409 `PeConflitoGravacao`); a primeira gravação de um formulário também passa por ela (dois primeiros cliques não viram dois registros). No catálogo do DF não há índice único de código: quem garante é a sequência.
  - `pe_deliberacao`: objeto (`petic`; `pdtic` na E7; sem FK), versão do objeto, envio (em e por), situação (`aguardando`, `aprovado`, `devolvido`; **token de concorrência**), decisão (em e por), ato (tipo, número, data), SEI e observação. CHECKs: decidida tem data e autor; aprovado tem número e data do ato; devolvido tem observação. `ux_pe_deliberacao_aguardando` (único parcial: um envio aguardando por objeto), `ix_pe_deliberacao_objeto` e `ix_pe_deliberacao_situacao`.
  - `pe_arquivo`: nome saneado, tipo (da extensão), tamanho, SHA-256, dono (`registro` e o id, ou os dois nulos) e o binário. **Table splitting**: o bytea fica na mesma linha, mapeado numa segunda entidade (`PeArquivoConteudo`, com a navegação só do lado dela), então consultar os metadados nunca traz o binário; só o download o seleciona. Como o table splitting deixa a coluna anulável no banco, `ck_pe_arquivo_conteudo` exige o binário e o tamanho igual ao registrado.
- **Motor de registros** (`PeRegistroService`, com `PeValores` e `PeRegistroModelo`; autorização dentro dele, pelo `PePermissionService`). Dono (`PeDono`): catálogo do DF ou uma versão do PETIC-DF; a seção precisa ser do escopo do dono, não apagada e não desligada (senão 404 `PeSecaoIndisponivel`; antes de o carregador trazer a versão 2 do modelo, 409 `PeModeloIndisponivel`, que é o intervalo da atualização). Fora do PDTIC, seção e campo aparecem pela situação geral; campo de ligação com seção some quando a seção ligada não aparece.
  - **Gravação**: só os campos visíveis entram; campo escondido (desligado ou apagado) **guarda o valor que já tinha** e não é exigido (decisão 13); chave que não existe na seção volta 400 no `Campos`; chave de campo escondido, apagado ou calculado é ignorada; campo de ligação em `Dados` é erro (vai em `Vinculos`). **PUT**: `Dados` presente troca os valores dos campos visíveis (chave ausente = vazio); `Dados` ausente ou nulo mantém; o mesmo para `Vinculos`. O estado final é validado (obrigatório que ficou vazio por mudança do modelo aparece no PUT).
  - **Tipos** (mensagens em linguagem simples, por chave, em `Campos`): texto curto e longo (sem espaço nas pontas, até o `max` do config ou 1000 e 50000), texto rico (objeto JSON até 200 mil caracteres, guardado como veio; a E4 detalha), número (min, max, casas), moeda (não negativa, até duas casas), percentual (0 a 100, até duas casas), data (`aaaa-mm-dd`, anos 1900 a 2199), sim ou não (booleano), lista (valor de opção ativa; a desativada só vale se já era a guardada), lista múltipla (lista de valores, sem repetir, guardada na ordem das opções; vazia = sem valor), arquivo (`{ ArquivoId }`; o nome guardado vem do arquivo). Vazio (nulo, texto em branco, lista vazia) não é guardado.
  - **Calculados**: calculados ao gravar e guardados no jsonb (o valor mandado pelo front é ignorado). Produto e soma ponderada usam só os campos visíveis (o escondido fica de fora do cálculo); subtração e nível de risco precisam dos dois (senão vazio); campo visível sem valor deixa o calculado vazio; lista entra pelo valor numérico da opção. O nível de risco guarda o **valor da opção** (texto), com o rótulo em `Rotulos`. Mudar o cálculo no modelo vale para os registros gravados depois.
  - **Código**: prefixo da seção e a sequência do dono, com dois dígitos no mínimo (OE01, N12, A100); formulário e tabela sem prefixo ficam sem código; o código já dado nunca muda (nem se o prefixo da seção mudar). **Ordem**: a nova vai para o fim; `PUT .../registros/ordem` recebe todos os ids da seção.
  - **Ligações**: `ligacao_secao` só com registros da seção ligada **no mesmo dono**; `ligacao_catalogo` com o catálogo (`petic_objetivo` e `petic_eixo` da **vigente**; `principio` do DF; `pgia_sistema` volta 400 até a E4); uma só quando `multipla` é falso; não liga a si mesmo. **Sem PETIC-DF vigente, a ligação com os catálogos dele fica opcional** (no motor e no `Obrigatorio` do GET).
  - **Apagar**: registro do sistema, 409 `PeRegistroDoSistema`; registro ligado por outro, 409 `PeRegistroLigado` com a mensagem de quem liga ("Não dá para apagar: este registro está ligado a IE01 (Indicadores estratégicos, PETIC-DF 1.0)..."); as ligações que saem dele vão junto.
  - **Respostas**: `Dados` (só os campos visíveis com valor), `Rotulos` (texto pronto: "R$ 1.234,50", "12,5%", "31/12/2026", "Sim", o rótulo da opção, os códigos das ligações), `Vinculos` (todo campo de ligação visível, com `RegistroId`, `Codigo` e `Resumo`, o campo principal do destino até 300 caracteres), `Sistema` e a auditoria. Formatação pt-BR feita à mão (`PeFormato`), sem depender da cultura do servidor.
  - Gravar um registro do PETIC-DF **toca a versão** (alterado em e por), com a situação como token de concorrência: gravar e enviar ao CGTIC ao mesmo tempo não passam os dois.
- **Referenciais semeados** (carregador, **versão 2** do `modelo-inicial.json`, com a lista nova `secoesForaDoPdtic`; idempotente; o que já existe não muda): 10 seções, 33 campos, 4 opções e 11 registros do sistema.
  - Catálogo do DF: `principio` (PR, obrigatória: texto, fundamento, "Pode ser critério de priorização?") e `diretriz_ciclo` (DC: ano do ciclo, diretriz ou prioridade, situação da deliberação com proposta, em deliberação, aprovada e devolvida, observação).
  - **Os 11 princípios do art. 4º** como registros do sistema PR01 a PR11: texto literal do inciso, sem o rótulo (igual à transcrição do front, com a pontuação final), fundamento "art. 4º, I, do Decreto nº 48.900/2026" e assim por diante, critério de priorização "sim"; a sequência do DF fica em 11 (o próximo acrescentado é PR12).
  - PETIC-DF: `petic_identidade` (formulário: missão, visão, valores), `petic_diretriz` (D), `petic_objetivo_programa` (OP), `petic_objetivo` (OE, com o objetivo do programa ligado), `petic_prioridade` (P, com objetivos ligados), `petic_indicador` (IE: nome, objetivo ligado, fórmula, unidade, linha de base, meta, prazo, fonte), `petic_iniciativa` (IN: descrição, objetivo ligado, responsável, prazo) e `petic_eixo` (EG: nome). **Decisão**: obrigatórias só as seções que o art. 11, § 1º, pede (diretrizes, objetivos, prioridades, indicadores e iniciativas); identidade, objetivos do programa e eixos ficam opcionais (o administrador muda).
  - O JSON ganhou a validação das seções fora do PDTIC (escopo, situação, sem trava, inciso ou níveis, um campo principal) e dos registros (só em tabela do DF com prefixo, código no formato, campos existentes; os valores passam pela mesma validação do motor e os obrigatórios precisam vir). Registro faltando é achado pelo código.
- **PETIC-DF** (`PePeticService`): um rascunho por vez (409 `PeVersaoEmAndamento` com rascunho ou versão em deliberação); `POST petic` cria vazio ou, com `CopiarDaVigente` (padrão verdadeiro), copia da vigente os registros (mesmos códigos e ordem), as ligações (a ligação com seção passa a apontar para a cópia; a com catálogo continua no mesmo item) e a sequência (código apagado na versão anterior não volta). Só o rascunho muda (409 `PeVersaoFechada` com a mensagem da situação). **Enviar** confere as pendências (vigência preenchida, seção obrigatória com registro, registro com campo obrigatório vazio pelo modelo de hoje; 400 `PePeticIncompleto` listando até 5) e cria a deliberação aguardando. **Apagar**: só o rascunho que nunca foi ao CGTIC (409 `PeVersaoJaEnviada`), com registros, ligações e sequências (os arquivos ficam).
- **Deliberações** (`PeDeliberacaoService`): fila com aguardando primeiro (por ordem de chegada) e depois as decididas (da mais nova); filtros por situação e objeto (fora do domínio = lista vazia); página saneada (20; até 100). **Decidir**: aprovado exige número e data do ato (data até hoje); devolvido exige observação; SEI no formato 00000-00000000/0000-00; decidida não muda (409 `PeDeliberacaoJaDecidida`). Aprovar marca a vigente anterior como substituída numa gravação e aprova a nova na seguinte, na mesma transação (o índice único não admite duas vigentes nem por um instante); devolver volta a versão a rascunho. `Titulo` "PETIC-DF 1.0" (a E7 põe "PDTIC SES 1.0" e `OrgaoSigla`). Deliberação de PDTIC volta 400 até a E7.
- **Planilhas** (`PePlanilhaService` e `PeXlsx`): colunas = código (tabela com prefixo) e os campos visíveis marcados "na planilha", na ordem, com os rótulos; ligações pelos códigos. **CSV** pelo escritor comum (UTF-8 com BOM, `;`, CRLF, proteção contra fórmula no texto digitado e nos rótulos), números com vírgula e sem milhar, datas dd/mm/aaaa. **XLSX** (Open XML SDK): uma aba por seção (nome do título, válido no Excel), cabeçalho em negrito com fundo e painel congelado, filtro, larguras pela largura do campo, números, moeda, percentual (o número como no sistema, com "%" no formato) e datas como números com formato, lista e sim ou não com validação de dados apontando para a aba oculta "Listas" (sem o limite de 255 caracteres), texto como inline string (nada vira fórmula), caracteres que o XML não aceita retirados, texto cortado em 32767; aba **Leia-me** com dono, versão e situação, título, vigência, data e hora da extração (Brasília), como ler e a ajuda de cada coluna. A completa só sai em XLSX (seções visíveis e "na planilha"); seção fora da planilha ou desligada, 404. Nomes: `PETIC-DF_<versao>_<secao>_<aaaa-mm-dd>.<ext>`, `PETIC-DF_<versao>_completa_<aaaa-mm-dd>.xlsx` e `DF_<secao>_<aaaa-mm-dd>.<ext>`. Conferido com o `OpenXmlValidator` do próprio SDK (Office 2016, sem erros) e aberto no LibreOffice.
- **CSV comum**: o escritor saiu do `CtrCsv` para `app/Helpers/CsvEscritor.cs` (`demanda_service.Helpers`), com a mesma lista de caracteres de fórmula; o `CtrCsv` escreve os mesmos bytes (o `test-contratacoes` segue com 493 verdes).
- **Arquivos** (`PeArquivoService`): envio multipart (campo `arquivo`, `[RequestSizeLimit]` de 26 MB) até 25 MB, imagens (PNG e JPEG) até 5 MB, lista fechada pdf, png, jpg e jpeg (os tipos do campo de arquivo), **conteúdo conferido pela assinatura** (PDF, PNG, JPEG), nome saneado (`PgiaDocumentoService.SanearNomeArquivo`), MIME da extensão (o do navegador é ignorado), SHA-256. Envia quem edita alguma coisa no módulo (pe_admin, pe_cgtic, pe_orgao e admin geral). O arquivo nasce sem dono e ganha dono quando um registro o usa num campo de arquivo; só serve o arquivo enviado pela mesma pessoa e ainda sem dono, do tipo e do tamanho do campo (o mesmo arquivo reenviado no mesmo registro continua valendo). **Baixa** quem pode ver o dono do registro (PETIC-DF e DF: qualquer papel, com a regra das versões para o órgão); sem dono, ou com o registro apagado, só quem enviou. O download manda `X-Content-Type-Options: nosniff`.
- **API** (JSON PascalCase; política `modulo:planejamento`; erros `{ Code, Message }` pelo `PeRespostas`, e a validação de registro com `Campos`):
  - `PeRegistrosController` (`api/planejamento`): `GET|POST petic/{peticId}/secoes/{secaoChave}/registros`, `PUT|DELETE .../registros/{id}`, `PUT .../registros/ordem`, e o mesmo em `df/secoes/{secaoChave}/registros`; `GET catalogos/{catalogo}`; `GET df/planilha/{secaoChave}?formato=` e o atalho `GET principios/planilha?formato=` (a mais que o contrato). POST devolve **201** com o registro; DELETE, **204**.
  - `PePeticController` (`api/planejamento/petic`): `GET` (da mais nova para a mais antiga, com a deliberação mais recente), `GET vigente` (**204** sem vigente), `GET {id}` (a mais que o contrato), `POST` (**201**), `PUT {id}` (parcial, como na E2), `POST {id}/enviar`, `DELETE {id}` (**204**), `GET {id}/planilha/{secaoChave}?formato=` e `GET {id}/planilha?formato=`.
  - `PeDeliberacoesController` (`api/planejamento/deliberacoes`): `GET ?Situacao=&ObjetoTipo=&Page=&PageSize=` (`PagedResponse`) e `POST {id}/decidir`.
  - `PeArquivosController` (`api/planejamento/arquivos`): `POST` (multipart, **201** com `{ Id, Nome, TipoMime, Tamanho }`) e `GET {id}`.
  - Os corpos são lidos à mão (`PeRegistroSalvarDTO.Ler`, `PeCorpo.Ler` e o `PeCorpoParcial` da E2): JSON que não é objeto, tipo errado ou texto que não é UTF-8 voltam 400 `PeDadosInvalidos` com mensagem, nunca o 400 genérico do model binding nem 500.
- **Permissões** (`PePermissionService`): `PodeLerReferenciais` (qualquer papel e admin geral), `PodeEditarReferenciais` (pe_admin e admin geral), `PodeVerDeliberacoes` (papéis globais e admin geral: pe_admin e pe_sgdi leem a fila), `PodeDecidirDeliberacao` (pe_cgtic e admin geral), `PodeEnviarArquivo` e **`PodeVerVersaoPetic`: papel de órgão só vê as versões aprovadas** (vigente e substituídas): a lista vem filtrada, e `GET petic/{id}`, os registros, as planilhas e os anexos de rascunho ou versão em deliberação dão 404 para ele.
- **Editor do modelo com dados (fecha as limitações da E2)**: campo com valor gravado em algum registro (no jsonb, ou ligações gravadas no campo de ligação) não muda de chave nem de tipo (409 `PeItemEmUso`, dizendo em quantos registros); campo de ligação com ligações gravadas não troca a seção nem o catálogo que liga (`multipla` muda); seção com registros não muda de chave nem de tipo; opção guardada num registro (lista, lista múltipla ou resultado do nível de risco) só é desativada, não apagada.
- **Erros** (faixa 1030 a 1049): `PeRegistroNaoEncontrado` 1030 (404), `PeRegistroInvalido` 1031 (400, com `Campos`), `PeRegistroDoSistema` 1032 (409), `PeRegistroLigado` 1033 (409), `PeFormularioJaPreenchido` 1034 (409), `PeSecaoIndisponivel` 1035 (404), `PePeticNaoEncontrado` 1036 (404; também versão que o papel de órgão não vê), `PeVersaoFechada` 1037 (409), `PeVersaoEmAndamento` 1038 (409), `PePeticIncompleto` 1039 (400), `PeDeliberacaoNaoEncontrada` 1040 (404), `PeDeliberacaoJaDecidida` 1041 (409), `PeDecisaoInvalida` 1042 (400), `PeArquivoInvalido` 1043 (400), `PeArquivoNaoEncontrado` 1044 (404), `PeCatalogoNaoEncontrado` 1045 (404), `PePlanilhaInvalida` 1046 (400), `PeVersaoJaEnviada` 1047 (409). Continuam valendo `PeSemPermissao` 1002 (403), `PeDadosInvalidos` 1012, `PeOrdemInvalida` 1017, `PeItemEmUso` 1023, `PeModeloIndisponivel` 1024 e `PeConflitoGravacao` 1025.
- **Regra do deploy (mantida):** nada no caminho de cada requisição lê tabela `pe_`. No intervalo (código da E3 publicado, migration não aplicada), o carregador versão 2 falha ao ler `pe_registro` e desfaz tudo (uma transação só): o modelo da E2 continua como estava, e ele tenta de novo sozinho. Os endpoints novos respondem 409 `PeModeloIndisponivel` com corpo; os da E1 e da E2 seguem normais. **Conferido num PostgreSQL de verdade (contêiner descartável)**: banco com as migrations até a E2 e o modelo versão 1 carregado pela própria E2; com o código da E3, `/me`, `meu-papel` e `GET modelo` em 200 e os endpoints novos em 409 com corpo; depois da migration da E3, o log mostra "modelo inicial versão 2 carregado (antes: 1). Novos: 0 níveis, 0 etapas, 0 passos, 10 seções, 33 campos, 4 opções, 0 configurações, 11 registros do sistema.", e o fluxo inteiro passou (princípios, rascunho, códigos, ligações, 409 de quem liga, 6 inclusões simultâneas, envio, decisão, cópia, substituição, exclusão em cascata do rascunho, planilhas, envio e download de arquivo com nosniff, conteúdo falso e extensão fora da lista recusados).
- **Testes:** `test-planejamento` com **331** (260 da E1 e da E2, 6 deles ajustados à versão 2 do modelo, e 71 novos: `PeReferenciaisCargaTest`, `PeRegistroMotorTest`, `PePeticTest`, `PeDeliberacaoTest`, `PePlanilhaTest` (o XLSX lido com o próprio Open XML SDK e conferido pelo `OpenXmlValidator`), `PeArquivoTest`, `PeReferenciaisControllerTest`, `PeModeloComDadosTest` e `PeMigrationReferenciaisTest`; base `PeReferenciaisTestBase`). O `test-pgia` continua com 588 (o mapa de controllers ganhou os 4 novos e o contexto do intervalo do deploy confere também `pe_registro` e `pe_petic`); o `test-contratacoes`, com 493.
- **Para a E4** (pontos de extensão): `PeDono` ganha o PDTIC (dono `"pdtic:ID"` na sequência); `RegistrosDo` passa a filtrar `pdtic_id` (e o catálogo do DF, `pdtic_id` nulo); a resolução da seção do PDTIC vem da trilha do órgão (nível e ajustes) no lugar da situação geral; o `ux_pe_registro_petic_codigo` ganha o par do PDTIC; as permissões do PDTIC (órgão no próprio) entram no `AbrirAsync` e no download dos anexos; o catálogo `pgia_sistema` e o texto rico completo ficam com ela. **A trilha da E2 ainda marca como obrigatório o campo de ligação com o PETIC-DF quando não há vigente** (o motor dispensa); alinhar na E4.
- **Limitações registradas:** arquivo que ninguém usa (ou cujo registro foi apagado) fica guardado, sem limpeza; o registro apagado sai de verdade (sem histórico do registro; o código não volta); o calculado só é recalculado quando o registro é gravado; a versão do PETIC-DF sobe só o número principal ("2.0"); o texto rico é validado só como objeto JSON.

### E4 · PDTIC dos órgãos: trilha, situação dos passos, comentários e planilhas (2026-09-24)

O órgão preenche o PDTIC dele passo a passo (as etapas 1 a 3 da trilha resolvida pelo nível), com o motor de registros da E3; o administrador do módulo e a SGDI comentam os passos e a equipe do órgão responde; as planilhas saem por seção, completas e consolidadas de todos os órgãos.

- **Tabelas** (migration **`20260924183900_PePdticTrilha`**, script revisado: `CREATE TABLE` de `pe_pdtic`, `pe_pdtic_passo` e `pe_comentario` com 7 índices delas; na tabela da E3 `pe_registro`, `ADD COLUMN pdtic_id bigint` nulável e sem default (não reescreve a tabela), `ck_pe_registro_dono` (`petic_id IS NULL OR pdtic_id IS NULL`), `ux_pe_registro_pdtic_codigo` (pdtic_id, secao_id, codigo) e `fk_pe_registro_pdtic` (CASCADE); `pgia_orgao` só é apontada por FK (RESTRICT); nenhuma tabela fora do módulo tocada; `has-pending-model-changes` limpo; o `PeMigrationPdticTest` trava o escopo, inclusive o Down):
  - `pe_pdtic`: órgão (FK `pgia_orgao`), versão (`"1.0"`; um ciclo novo depois de um encerrado vira `"2.0"`; única por órgão), situação (`em_elaboracao`, `em_aprovacao`, `devolvido`, `aprovado`, `publicado`, `em_acompanhamento`, `encerrado`, `substituido`; **token de concorrência**), vigência (`DateOnly`, copiada do passo 1.1; CHECK fim não antes do início), `registrado_externamente` (E7), `anterior_id` (o PDTIC anterior do órgão) e auditoria. **`ux_pe_pdtic_atual`**: único parcial em `orgao_id` com a situação fora de `encerrado` e `substituido` (um PDTIC atual por órgão; dois cliques em "abrir" não viram dois).
  - `pe_pdtic_passo`: chave (pdtic, passo), `nao_se_aplica`, `justificativa` (até 1000), `marcado_em/por`; CHECK `nao_se_aplica = (justificativa IS NOT NULL)`. Desfazer mantém a linha com falso (quem desfez e quando).
  - `pe_comentario`: PDTIC (CASCADE), passo (RESTRICT), `pai_id` (a resposta aponta para o comentário principal; CASCADE), texto (até 2000), autor (e-mail e nome), `criado_em`, `resolvido_em/por` (CHECK: os dois juntos; a resposta não é resolvida sozinha).
- **Dono PDTIC no motor** (`PeDono.DoPdtic`; sequência `"pdtic:ID"`; `RegistrosDo` filtra `pdtic_id`, e o catálogo do DF passou a exigir `petic_id` e `pdtic_id` nulos): a seção e os campos saem da **trilha do órgão** (`PeTrilhaOrgao`: nível escolhido ou padrão e ajustes, pelo `PeTrilhaResolver` da E2), com a obrigatoriedade do nível; seção fora da trilha, 404 `PeSecaoIndisponivel`; campo desligado some no GET, é ignorado no POST e no PUT e **guarda o valor** (volta quando o nível ou o ajuste o religa). **Ler**: quem vê o órgão (papéis globais, admin geral, equipe e consulta do próprio órgão; outro órgão 403). **Gravar**: `pe_orgao` do próprio órgão e admin geral (`PodeEditarPdtic`), só em `em_elaboracao` ou `devolvido` (senão 409 `PePdticFechado`, com a mensagem da situação); gravar toca o PDTIC (alterado em e por, com a situação como token). A vigência da seção `abrangencia` vai para o `pe_pdtic` a cada gravação (nula quando o registro sai); fim antes do início volta 400 no campo `vigencia_fim` (regra de qualquer seção com os campos `vigencia_inicio` e `vigencia_fim`).
- **Pendências da E3 fechadas:**
  - **Catálogo `pgia_sistema`**: `GET catalogos/pgia_sistema?pdticId=` devolve os sistemas do inventário do PGIA do órgão daquele PDTIC (`[{ Id, Codigo: null, Rotulo }]`, por nome; sem `pdticId`, 400 `PeDadosInvalidos`; quem não vê o órgão, 403). No campo `ligacao_catalogo` com esse catálogo, **os ids ficam no jsonb do registro** (lista de números), porque `pe_vinculo` só liga registros; no corpo e na resposta continuam em `Vinculos` (`RegistroId` = id do sistema, `Codigo` nulo, `Resumo` = denominação, na ordem escolhida), e a planilha mostra os nomes. Só sistemas do órgão do PDTIC; fora do PDTIC, 400 no campo. O editor do modelo conta esses valores como dados gravados (o campo não troca de chave, de tipo nem de catálogo). O PGIA só é lido (nada no PGIA mudou).
  - **Texto rico validado** (`PeTextoRico`, chamado pelo `PeValores`): lista fechada do contrato (nós `doc`, `paragraph`, `heading` com `level` 3 ou 4, `bulletList`, `orderedList`, `listItem`, `table`, `tableRow`, `tableHeader`, `tableCell`, `image`, `hardBreak`, `text`; marcas `bold`, `italic`, `underline`, `link`), cada nó onde o editor o põe (lista com item, tabela com linha, célula com bloco). Atributos: só os conhecidos com valor válido (`level`; `start` e `type` da lista numerada; `colspan`, `rowspan` e `colwidth`; `src`, `alt`, `title`, `width` e `height` da imagem; `href`, `target` `_blank` e `rel` do link); os outros (alinhamento, classe, estilo) **saem sem erro**, e o JSON é guardado já limpo. Recusa com 400 no campo: nó ou marca fora da lista (a mensagem diz o recurso: "tachado", "citação", "bloco de código"), título fora de 3 e 4, link que não é endereço completo `http` ou `https`, imagem que não é `api/planejamento/arquivos/{id}` (com ou sem a barra no começo; guardada sem), estrutura fora do lugar, mais de 25 níveis de nó (o leitor de JSON do ASP.NET para em 64 níveis de JSON, cerca de 30 de nó: acima disso, o 400 genérico do model binding). Documento sem letra nem imagem conta como vazio. **Imagens**: PNG ou JPEG do módulo, enviadas por quem grava e ainda sem dono (ou já deste registro); ao gravar, **o registro vira o dono** e a imagem abre para quem vê o dono (no PDTIC, quem vê o órgão; o `PeArquivoService` ganhou essa regra).
  - **Trilha da E2 sem PETIC-DF vigente**: o `GET modelo/trilha` (que passou a usar o `PeTrilhaOrgao`) vem com `Obrigatorio` falso nas ligações com `petic_objetivo` e `petic_eixo` enquanto não há versão vigente, como o motor já fazia; com a vigente, volta a obrigatória no Intermediário e no Avançado (e o passo com registro sem a ligação volta a pendente).
- **PDTIC** (`PePdticService`): **abrir** (`POST pdtic`): `pe_orgao` no próprio órgão (outro órgão 403; sem órgão 404 `PeOrgaoNaoEncontrado`) e admin geral no `OrgaoId` informado (sem ele, 400 `PeOrgaoObrigatorio`); os outros papéis, 403; órgão inativo, 404; já há um atual, 409 `PePdticJaExiste` (a corrida entre dois cliques cai no índice único e volta 409 `PeConflitoGravacao`). **`GET pdtic/atual?orgaoId=`**: sem `orgaoId`, o órgão da própria pessoa (papel global sem órgão, 400 `PeOrgaoObrigatorio`); 204 sem corpo quando não há atual. **`GET pdtic?Situacao=&Filtro=&Page=&PageSize=`** (papéis globais, inclusive `pe_cgtic`, e admin geral): `PagedResponse` dos atuais, por sigla; situação fora do domínio devolve lista vazia; filtro por sigla ou nome. A resposta tem o nível de **hoje** do órgão e `PodeEditar` (quem grava e situação aberta).
- **Situação dos passos** (`GET pdtic/{id}/situacao`, e `CalcularSituacaoAsync` para a E7 e a E8): todos os passos visíveis, na ordem e com a numeração da trilha (a mesma do `GET modelo/trilha`). Ordem da decisão: `atencao` quando há comentário principal aberto no passo; senão `nao_se_aplica` quando a marca vale (o passo continua opcional, aceita e não é travado; marca que deixou de valer é ignorada e volta a valer com o ajuste); senão, no tipo `dados`, `feito` quando todas as seções visíveis do passo estão completas (obrigatória com pelo menos um registro; todo registro com os obrigatórios de hoje, as ligações pela `pe_vinculo` e as do PGIA pelo jsonb) e `pendente` no contrário (passo sem seção obrigatória fica feito); no tipo `conferencia_temas`, `feito` quando cada tema do decreto tem ação ou justificativa; nos outros tipos (documento, fluxo, aprovação, envio, deliberação, publicação, monitoramento), `continuo`. `ProximoPasso` = o primeiro `pendente` ou `atencao`, ou nulo. Uma consulta pega todos os registros do PDTIC (`AnalisarAsync` do motor, que o `PendenciasAsync` da E3 também usa).
- **Avisos do guia** (não bloqueiam; `Avisos` do passo, texto pronto): "As fraquezas D01 e D02 ainda não têm necessidade de TIC ligada..." e "A ameaça AM01 ainda não tem risco ligado..." **no passo da SWOT** (com o número do passo onde se faz a ligação, pela trilha), só quando o campo de ligação (`necessidades.fraqueza`, `riscos.ameaca`) aparece para o órgão; e "A equipe de elaboração tem só pessoas da área de TIC..." no passo da equipe, quando `tipo_area` aparece e todos os preenchidos são `tic`. Passo marcado "não se aplica" não avisa. As chaves usadas ficam em `PeDominios.ChavePdtic`.
- **Não se aplica** (`PUT/DELETE pdtic/{id}/passos/{passoId}/nao-se-aplica`, com o id do passo da trilha): equipe do órgão e admin geral, PDTIC aberto; passo fora da trilha, 404 `PePassoIndisponivel`; travado, que não aceita ou obrigatório no nível e nos ajustes, 409 `PeNaoSeAplicaRecusado` com o motivo; justificativa vazia, 400 `PeJustificativaObrigatoria` (até 1000). Devolve a situação do passo. **No modelo inicial nenhum passo é opcional** (a E2 semeou "sim" como obrigatório): o "não se aplica" só aparece quando o administrador deixa um passo opcional no nível ou num ajuste do órgão.
- **Temas das ações** (`GET pdtic/{id}/temas`): os três temas travados de `acoes.tema` (V `seguranca`, VI `transformacao_digital`, IX `governanca_dados`, em `PeDominios.TemaDecreto`), com o rótulo de hoje, as ações do PDTIC com o tema (`RegistroId`, `Codigo`, `Descricao` inteira, `Situacao` = **rótulo** da situação da ação) e a justificativa de tema sem ação, lida da seção formulário **`temas_sem_acao`** do passo 3.4 (campos `justificativa_seguranca`, `justificativa_transformacao` e `justificativa_dados`, gravados pelo endpoint normal de registros; campo desligado não conta).
- **Sistemas de IA do PGIA** (`GET pdtic/{id}/ia/pgia`, quem vê o órgão): `pgia_sistema_ia` do órgão do PDTIC, por nome: `Nome` (denominação), `Finalidade`, `Classificacao` (`classificacao_risco_atual`), `Base` (`enquadramento_legal`, o artigo e o inciso da classificação; nulo no baixo risco) e `Situacao` (`status_ciclo_vida`). As aquisições planejadas são registros da seção `aquisicoes_ia`.
- **Comentários** (`PeComentarioService`, quadro de papéis do plano, seção 4.1): **lê** quem vê o órgão (consulta e Secretaria do CGTIC inclusive); **comenta** (primeiro nível) `pe_admin`, `pe_sgdi` e admin geral (a Secretaria do CGTIC não); **responde** a equipe do órgão (e o admin geral); **resolve** a equipe do órgão, o autor (pelo e-mail) e o admin geral (resolver de novo não muda nada). Um nível de resposta (responder a uma resposta ou com outro passo, 400 `PeComentarioInvalido`); comentário resolvido não recebe resposta (409 `PeComentarioResolvido`); passo do comentário precisa estar na trilha do órgão (404 `PePassoIndisponivel`). `GET` devolve os comentários principais em ordem de criação com as `Respostas` dentro (`?passoId=` filtra); `POST` devolve **201 com a conversa inteira** (o comentário principal, com a resposta nova); `resolver` devolve a conversa.
- **Planilhas** (`PePlanilhaService`, agora com o `IPePermissionService`): **do PDTIC** (quem vê o órgão), por seção (`?formato=csv|xlsx`, padrão xlsx) e completa (só xlsx; uma aba por seção visível e "na planilha", na ordem da trilha), com as colunas do nível do órgão e a Leia-me com órgão, PDTIC (versão e situação), nível, vigência e data; nomes `PDTIC_<SIGLA>_<secao>_<aaaa-mm-dd>.<ext>` e `PDTIC_<SIGLA>_completa_<data>.xlsx` (sigla só com letras, números, hífen e sublinhado). **Consolidado** (papéis globais e admin geral; os outros, 403): uma linha por registro de todos os PDTICs atuais cujo órgão vê a seção, por sigla; colunas Órgão, Sigla, Nível (o de hoje), Versão do PDTIC e Situação do PDTIC, depois o Código e a **união** dos campos visíveis em qualquer nível ou para qualquer órgão incluído (célula vazia quando o campo não aparece para o órgão); o completo (só xlsx) tem uma aba por seção do PDTIC que aparece em algum nível ou órgão; nomes `PDTIC_consolidado_<secao>_<data>.<ext>` e `PDTIC_consolidado_completa_<data>.xlsx`. O consolidado busca os registros de uma seção de todos os órgãos de uma vez (`ExportarDosPdticsAsync`: poucas consultas por seção, não por órgão). Conferido com o `OpenXmlValidator` (Office 2016, sem erros).
- **API** (`Controllers/Planejamento/PePdticController.cs`, rota `api/planejamento`, política `modulo:planejamento`; e as rotas do PDTIC no `PeRegistrosController`): `GET pdtic/atual`, `POST pdtic` (**201**; corpo vazio vale como `{ OrgaoId: null }`), `GET pdtic/{id}`, `GET pdtic`, `GET pdtic/{id}/situacao`, `PUT|DELETE pdtic/{id}/passos/{passoId}/nao-se-aplica`, `GET pdtic/{id}/temas`, `GET pdtic/{id}/ia/pgia`, `GET|POST pdtic/{id}/comentarios` (POST **201**), `POST comentarios/{id}/resolver`, `GET pdtic/{id}/planilha/{secaoChave}`, `GET pdtic/{id}/planilha`, `GET consolidado/{secaoChave}`, `GET consolidado`; `GET|POST pdtic/{pdticId}/secoes/{secaoChave}/registros`, `PUT|DELETE .../registros/{id}` e `PUT .../registros/ordem` (o contrato da E3), e `GET catalogos/{catalogo}?pdticId=`.
- **Erros** (faixa 1050 a 1069): `PePdticNaoEncontrado` 1050 (404), `PePdticJaExiste` 1051 (409), `PePdticFechado` 1052 (409), `PePassoIndisponivel` 1053 (404), `PeNaoSeAplicaRecusado` 1054 (409), `PeComentarioNaoEncontrado` 1055 (404), `PeComentarioInvalido` 1056 (400), `PeComentarioResolvido` 1057 (409). Continuam valendo os anteriores (`PeSemPermissao` 1002, `PeOrgaoNaoEncontrado` 1011, `PeDadosInvalidos` 1012, `PeJustificativaObrigatoria` 1020, `PeOrgaoObrigatorio` 1021, `PeModeloIndisponivel` 1024, `PeConflitoGravacao` 1025, `PeRegistroInvalido` 1031 com `Campos`, `PeSecaoIndisponivel` 1035, `PePlanilhaInvalida` 1046).
- **Carregador sem versão nova**: a seção 8 do plano já estava inteira no modelo inicial da E2 (conferida seção por seção), inclusive o dicionário de nomes `nomes` do passo 1.2 (para os marcadores da E5) e `temas_sem_acao` do passo 3.4; o `seed_modelo_versao` continua 2.
- **Regra do deploy (mantida):** nada no caminho de cada requisição lê tabela `pe_`. Como `pe_registro` ganhou coluna, no intervalo (código da E4 publicado, migration não aplicada) toda leitura de registro bate em coluna inexistente (42703): o `PeBanco.TabelaAusente` passou a tratar 42703 além de 42P01, e esses endpoints (os da E4 e, da E3, os de registros, catálogos, planilhas, cópia e envio do PETIC-DF e o download de anexo) respondem **409 `PeModeloIndisponivel` com corpo**; o carregador (versão 2 já carregada) só lê a versão. **Conferido num PostgreSQL de verdade (contêiner descartável)**: banco com as migrations até a E3 e o modelo versão 2 carregado pelo código da E3 (exportado por `git archive`), depois o código da E4: `/me`, `meu-papel`, `GET modelo`, `GET modelo/trilha`, órgãos, lista do PETIC-DF e deliberações em 200; registros do DF, catálogo e os endpoints da E4 em 409 com corpo. Depois da migration: o fluxo inteiro passou (abrir, 409 do segundo, lista, situação com 23 passos, vigência copiada e invertida recusada, N01 e M01 ligados, 409 ao apagar o ligado, consulta sem gravar, seção fora do Básico 404, imagem enviada e usada no texto rico, texto fora da lista 400, download pela consulta com `nosniff`, "não se aplica" recusado, ação e temas, PGIA e catálogo, comentário, resposta, atenção e resolução, planilhas do órgão e consolidadas, 403 do consolidado para o órgão, trilha sem obrigatoriedade do PETIC-DF), seis aberturas simultâneas do mesmo órgão deram um PDTIC e cinco 409, o Down e o Up de novo rodaram com dados, e a regressão da E3 (princípios, rascunho, OE01 e IE01 ligados, envio incompleto, planilha, exclusão do rascunho) passou.
- **Testes:** `test-planejamento` com **410** (331 da E1 a E3, 2 ajustados: a trilha sem PETIC vigente e o catálogo do PGIA que passou a existir; e 79 novos: `PePdticTest`, `PePdticRegistrosTest`, `PePdticSituacaoTest`, `PeComentarioTest`, `PePdticPlanilhaTest`, `PePdticControllerTest`, `PeTextoRicoTest` e `PeMigrationPdticTest`; base `PePdticTestBase`). O `test-pgia` continua com 588 (o mapa de controllers ganhou o `PePdticController` e o contexto do intervalo do deploy confere também `pe_pdtic` e `pe_comentario`); o `test-contratacoes`, com 493.
- **Limitações registradas:** a situação é por tipo de passo (os passos de dados das etapas 4 a 7 também ficam pendentes até serem preenchidos; a E7 trata o acompanhamento); o Down da migration não apaga os registros do PDTIC (ficam em `pe_registro` sem dono, fora de qualquer tela); a imagem de um texto rico pertence a um registro só (a mesma imagem em outro registro precisa ser enviada de novo); arquivo e imagem sem uso continuam sem limpeza; comentário não se edita nem se apaga.
