# SGDP-Service - Variáveis de Ambiente
# Copie este arquivo para .env e preencha com valores reais
# NUNCA commite o arquivo .env com credenciais reais!

# ====================
# DATABASE CONFIGURATION
# ====================

# PostgreSQL - Desenvolvimento Local
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=postgres

# Connection Strings (preenchidas automaticamente no appsettings ou user secrets)
# ConnectionStrings__PostgreSql=Host=localhost;Port=5432;Database=postgres;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
# ConnectionStrings__PostgreSqlDocker=Host=postgres;Port=5432;Database=postgres;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}

# ====================
# PGADMIN CONFIGURATION
# ====================

PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=your_pgadmin_password

# ====================
# APPLICATION SETTINGS
# ====================

# Mode: "container" para Docker, vazio para local
MODE=container

# ASP.NET Core URLs
ASPNETCORE_URLS=http://+:5148
ASPNETCORE_ENVIRONMENT=Development

# ====================
# JWT AUTHENTICATION
# ====================

# Chave JWT (mínimo 256 bits / 32 caracteres)
# Gere com: openssl rand -base64 32
Auth__Key=your_jwt_secret_key_minimum_256_bits_long

# JWT Settings (opcional, se não estiver em appsettings)
# Auth__Issuer=https://localhost:5148/
# Auth__Audience=https://localhost:5148/
# Auth__ExpireMinutes=10
# Auth__RefreshTokenExpireMinutes=120

# ====================
# PRODUCTION NOTES
# ====================

# Em produção, use variáveis de ambiente do sistema ou secrets manager
# Nunca armazene credenciais em arquivos de código fonte ou .env commitados
#
# Para .NET User Secrets (desenvolvimento):
# dotnet user-secrets init
# dotnet user-secrets set "ConnectionStrings:PostgreSql" "Host=..."
# dotnet user-secrets set "Auth:Key" "your-secret-key"
